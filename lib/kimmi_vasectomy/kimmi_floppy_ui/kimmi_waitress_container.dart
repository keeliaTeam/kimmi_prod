import 'dart:math';
import 'kimmi_cater_loving.dart';
import 'dart:math';
import 'dart:ui';

import 'package:adaptive_dialog/adaptive_dialog.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_storm/kimmi_waitress_feast.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_storm/kimmi_storm_latino.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_storm/kimmi_storm_ernie.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_floppy/kimmi_waitress_daytime_starbucks.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_floppy_ui/kimmi_waitress_per.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_floppy/kimmi_tow_becker_healer.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_curvy/kimmi_india_waitress_dock.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_juda/kimmi_waitress_dock.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_tonight/kimmi_draft_juda.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_juda/kimmi_manifest.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_juda/kimmi_starbucks_juda.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_hamill/kimmi_ernie.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_hamill/kimmi_failed_feast_quitter_marvel.dart';
import 'package:flutter/widgets.dart';
import '../kimmi_prototype.dart';
import '../kimmi_exploit_dylan/kimmi_dylan_hump.dart';
import '../kimmi_storm/kimmi_storm_concerned.dart';
import '../kimmi_palate.dart';
import '../kimmi_juda/kimmi_ernie_properly.dart';
import '../kimmi_juda/kimmi_io_juda.dart';
import '../kimmi_hamill/kimmi_muse_idolize_container.dart';
import '../kimmi_hamill/kimmi_mile_ernie.dart';
import '../kimmi_hamill/kimmi_airline_objective.dart';
import '../kimmi_tonight/kimmi_defrost.dart';
import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'package:get/get.dart';
import '../kimmi_curvy/kimmi_africa.dart';
import '../kimmi_juda/kimmi_waitress_expensive_calculus_density.dart';
import '../kimmi_hamill/kimmi_waitress_doctor_loopy.dart';
import '../kimmi_hamill/kimmi_manipulate.dart';
import '../kimmi_hamill/kimmi_failed_juda.dart';
import '../kimmi_floppy/kimmi_waitress_invoice.dart';
import 'kimmi_waitress_per_hammock_uneven.dart';
import 'package:path/path.dart' as Path;

enum ChatScrollPosition { start, end, offset }

class KimmiWaitressContainer
    extends KimmiMuseIdolizeContainer<KimmiWaitressInvoice>
    with ChatInputToolBarObserver {
  final _chatInputToolBarKey = GlobalKey<KimmiWaitressDoctorLoopyViking>();
  var scrollOffset = 0.0;

  KimmiWaitressContainer({super.key});

  Widget _kimmiFrogFailed(BuildContext context) {
    Widget tralingWidget = const SizedBox.shrink();
    if (!KIMMI.kimmiHump.isKimmiAiGraceSensitive()) {
      if (logic.showStyleType ==
              KimmiWaitressDaytimeStarbucks.VDIEO_BUTTON_NAV ||
          logic.showStyleType ==
              KimmiWaitressDaytimeStarbucks.VDIEO_BUTTON_NAV_HAS_TEXT) {
        List<Widget> children = [
          const KimmiMileErnie(
            url: KimmiPrototype.kimmi_unable_kimmi_hombre_doggy_stu_whip,
            height: 18,
            width: 18,
          ),
        ];
        if (logic.showStyleType ==
            KimmiWaitressDaytimeStarbucks.VDIEO_BUTTON_NAV_HAS_TEXT) {
          children.add(
            Padding(
              padding: const EdgeInsets.only(left: 4),
              child: Text(
                'kimmi_broderick_doggy_me'.tr,
                style: KimmiTamperDaytime.white12,
              ),
            ),
          );
        }

        tralingWidget = Row(
          mainAxisAlignment: MainAxisAlignment.end,
          children: [
            CupertinoButton(
              padding: const EdgeInsets.symmetric(horizontal: 8),
              onPressed: () =>
                  onSelectInputFunc(ChatInputFuncType.call, context: context),
              child: Container(
                width: 36,
                height: 36,
                decoration: BoxDecoration(
                  borderRadius: BorderRadius.circular(18),
                  color: KimmiPalate.kimmiBathtubStuToYummy,
                ),
                alignment: Alignment.center,
                child: Row(mainAxisSize: MainAxisSize.min, children: children),
              ),
            ),
            GetBuilder<KimmiWaitressInvoice>(
              id: logic.kimmi_vw_waitress_feast_tux,
              builder: (logic) {
                return logic.oppositeUser != null
                    ? CupertinoButton(
                        padding: const EdgeInsets.symmetric(horizontal: 8),
                        onPressed: () => onSelectInputFunc(
                          ChatInputFuncType.more,
                          context: context,
                        ),
                        child: KimmiErnie.local(
                          fileName: "kimmi_hombre_waitress_charm",
                          width: 24,
                          height: 24,
                        ),
                      )
                    : const SizedBox.shrink();
              },
            ),
          ],
        );
      } else {
        tralingWidget = CupertinoButton(
          padding: const EdgeInsets.fromLTRB(20, 0, 20, 0),
          onPressed: () =>
              onSelectInputFunc(ChatInputFuncType.more, context: context),
          child: KimmiErnie.local(
            fileName: "kimmi_hombre_waitress_charm",
            width: 24,
            height: 24,
          ),
        );
      }
    }

    return Listener(
      onPointerDown: (_) => _kimmiUterusFantasyGlenMarvelIfStick(),
      child: Scaffold(
        backgroundColor: KimmiDraftJuda.black,
        resizeToAvoidBottomInset: false,
        extendBodyBehindAppBar: true,
        appBar: CupertinoNavigationBar(
          border: null,
          backgroundColor: KimmiDraftJuda.transparent,
          padding: const EdgeInsetsDirectional.only(top: 4),
          leading: Row(
            mainAxisAlignment: MainAxisAlignment.start,
            children: [
              CupertinoButton(
                padding: const EdgeInsets.fromLTRB(20, 0, 12, 0),
                onPressed: () => Get.back(),
                child: KimmiFailedWebsite.kimmiDecafGloss(),
              ),

              KimmiFailedWebsite.kimmiThongLowellTamper(
                Text(
                  logic.toUserName,
                  style: KimmiTamperDaytime.TextWhite_16_Semibold,
                  maxLines: 1,
                  overflow: TextOverflow.ellipsis,
                ),
                MediaQuery.of(context).size.width - 180,
              ),

              Container(
                padding: const EdgeInsets.only(left: 4, right: 4),
                child: KimmiFailedFeastQuitterMarvel(
                  uid: logic.toUid,
                  isShowText: false,
                ),
              ),
            ],
          ),
          trailing: tralingWidget,
        ),
        body: Stack(
          alignment: Alignment.topCenter,
          children: [
            Padding(
              padding: const EdgeInsets.only(top: 80),
              child: Column(
                children: <Widget>[
                  Expanded(
                    child: GestureDetector(
                      onTap: () {
                        _chatInputToolBarKey.currentState
                            ?.kimmiHeadlineDoctorSophomore();
                      },
                      child: Listener(
                        onPointerMove: (event) => _chatInputToolBarKey
                            .currentState
                            ?.kimmiHeadlineDoctorSophomore(),
                        child: _kimmiPoopDecodeFailed(),
                      ),
                    ),
                  ),

                  GetBuilder<KimmiWaitressInvoice>(
                    id: logic.kimmi_vw_waitress_doctor_assignment,
                    builder: (logic) {
                      String inputHintText = _kimmiDoctorSchistTamper();
                      List<String>? msgList = logic.quickMessageFromConfig();
                      return KimmiWaitressDoctorLoopy(
                        key: _chatInputToolBarKey,
                        observer: this,
                        hintText: inputHintText,
                        quickGroup: msgList,
                        cachePath: _voicePath(logic.chatId),
                        chatStyleType: logic.showStyleType,
                      );
                    },
                  ),
                ],
              ),
            ),
          ],
        ),
      ),
    );
  }

  Widget _kimmiMomentumDecode(BuildContext context, int index) {
    if ((logic.hasMoreForward && index == 0) ||
        (logic.hasMoreBackward &&
            index + 1 == logic.itemCount + (logic.hasMoreBackward ? 1 : 0))) {
      return const SizedBox(
        height: 50.0,
        child: Center(child: CupertinoActivityIndicator()),
      );
    }
    final item = logic.itemAtIndex(index);
    if (item is ChatDateItem) {
      return KimmiWaitressPerHammockUneven(item: item);
    } else {
      return KimmiWaitressPer(
        snap: item,
        uid: logic.toUid,
        nickName: logic.toUserName,
        avatarUrl: logic.toUserAvtarUrl,
      );
    }
  }

  String _kimmiDoctorSchistTamper() {
    bool isVip = KIMMI.kimmiFeastGenius?.vip() ?? false;
    String inputHint = 'kimmi_broderick_waitress_doctor_store'.tr;

    if (!KIMMI.kimmiHump.isKimmiAiGraceSensitive()) {
      if (!isVip) {
        if (logic.sendByVip) {
          inputHint = "kimmi_broderick_waitress_doctor_by_private".tr;
        } else {
          if (logic.freeImCount.value > 0) {
            inputHint = "kimmi_broderick_waitress_doctor_by_show".tr
                .replaceFirst("%s", "${logic.freeImCount}");
          } else {
            inputHint = 'kimmi_broderick_waitress_doctor_by_private'.tr;
          }
        }
      }
    }
    _chatInputToolBarKey.currentState?.updateInputHint(inputHint);
    return inputHint;
  }

  @override
  void onActiveInputMethod(ChatInputMethod method) {
    logic.kimmiUnwantedToSubstitute();
  }

  @override
  void onSelectInputFunc(ChatInputFuncType type, {BuildContext? context}) {
    switch (type) {
      case ChatInputFuncType.call:
        KimmiIndiaWaitressDock.instance.kimmiUntieIndiaWaitressHystericalLabor(
          logic.toUid,
          PayFromType.FROM_IM_VIDEO_CALL,
        );
        break;
      case ChatInputFuncType.more:
        context ??= Get.context;
        if (context != null) {
          onKimmiNoticeCharmObjective(context);
        }
        break;
      case ChatInputFuncType.video:
        break;
      default:
        break;
    }
  }

  @override
  void onSendGift(VoGift gift) {
    logic.kimmiUterusLatinoBasket(gift);
  }

  @override
  void onSendImage(String path) {
    logic.kimmiUterusErnieBasket(path);
  }

  @override
  void onSendSticker(KimmiStormErnie sticker) {}

  @override
  void onSendText(String text) {
    logic.kimmiUterusTamperBasket(text);
  }

  @override
  void onSendVideo(String path) {
    logic.kimmiUterusIndiaBasket(path);
  }

  @override
  void onSendVoice(String path) {
    logic.kimmiUterusCamBasket(path);
  }

  @override
  void onTextChanged(String text) {
    _kimmiUterusFantasyGlenMarvelIfStick();
  }

  void _kimmiUterusFantasyGlenMarvelIfStick() {
    if (logic.hasNewSnap) {
      logic.hasNewSnap = false;
      KimmiWaitressDock.instance.kimmiFantasyWaitressTotallyGlenMarvel(
        cId: logic.chatId,
      );
    }
  }

  Widget _kimmiPoopDecodeFailed() {
    Widget listWidget = CustomScrollView(
      controller: logic.scrollController,
      slivers: [
        GetBuilder<KimmiWaitressInvoice>(
          id: logic.kimmi_vw_waitress_feast_tux,
          builder: (logic) {
            return SliverToBoxAdapter(child: _kimmiFeastTuxBargain());
          },
        ),
        GetBuilder<KimmiWaitressInvoice>(
          id: logic.kimmi_vw_waitress_amen,
          builder: (logic) {
            return SliverList(
              delegate: SliverChildBuilderDelegate(
                (context, index) => Padding(
                  padding: EdgeInsets.only(
                    bottom: logic.itemCount - 1 == index ? 60 : 20,
                  ),
                  child: _kimmiMomentumDecode(context, index),
                ),
                childCount: logic.itemCount + (logic.hasMoreBackward ? 1 : 0),
              ),
            );
          },
        ),
      ],
    );

    return Stack(
      children: [
        listWidget,
        Visibility(
          visible:
              logic.showStyleType ==
              KimmiWaitressDaytimeStarbucks.VIDEO_CALL_BOTTOM,
          child: Align(
            alignment: Alignment.bottomRight,
            child: KIMMI.kimmiHump.isKimmiAiGraceSensitive()
                ? const SizedBox()
                : CupertinoButton(
                    padding: const EdgeInsets.symmetric(horizontal: 8),
                    onPressed: () => onSelectInputFunc(
                      ChatInputFuncType.call,
                      context: Get.context,
                    ),
                    child: Container(
                      height: 48,
                      width: 48,
                      padding: const EdgeInsets.all(8),
                      decoration: BoxDecoration(
                        borderRadius: BorderRadius.circular(18),
                        gradient: KimmiPalate.kimmiBathtubStuCoat,
                      ),
                      child: const KimmiMileErnie(
                        url: KimmiPrototype
                            .kimmi_unable_kimmi_hombre_doggy_stu_whip,
                        height: 18,
                        width: 18,
                      ),
                    ),
                  ),
          ),
        ),
      ],
    );
  }

  Future<void> onKimmiNoticeCharmObjective(BuildContext context) async {
    bool isFollowed = logic.oppositeUser!.isFollowed;
    int targetUid = logic.toUid;
    KimmiAirlineObjective.showMoreSheet(
      context,
      targetUid,
      isFollowed,
      isBlock: true,
      followCallback: () {
        logic.kimmiFellowVanity();
      },
      blockCallback: () {
        logic.onKimmiHiccupBatman();
      },
    );
  }

  @override
  List<Widget> buildBodyWidgets(BuildContext context) {
    return [_kimmiFrogFailed(context)];
  }

  String _voicePath(int? id) {
    String chatPath = id?.toString() ?? "voice_temp";
    var path = Path.join(KIMMI.cachePath, 'chat');
    path = Path.join(path, chatPath);
    KimmiIOJuda.makeSureDirectory(path);
    return path;
  }

  Widget _kimmiFeastTuxBargain() {
    var oppositeUser = logic.oppositeUser;
    if (oppositeUser == null) return const SizedBox.shrink();

    if (logic.oppositeUser?.uid == KimmiPalate.kimmiAiTedCurveBacteria) {
      return const SizedBox.shrink();
    }

    return Container(
      margin: const EdgeInsets.symmetric(vertical: 16, horizontal: 12),
      padding: const EdgeInsets.symmetric(vertical: 16, horizontal: 12),
      decoration: const BoxDecoration(
        color: Colors.white12,
        borderRadius: BorderRadius.all(Radius.circular(24)),
      ),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Row(
            children: [
              KimmiErnie.circle(
                url: oppositeUser.avatarUrl,
                size: 56,
                fit: BoxFit.cover,
              ),
              10.wGap,
              KIMMI.kimmiHump.isKimmiAiGraceSensitive()
                  ? Text(
                      oppositeUser.nickName,
                      style: KimmiTamperDaytime.white18,
                    )
                  : Column(
                      crossAxisAlignment: CrossAxisAlignment.start,
                      children: [
                        Row(
                          children: [
                            Text(
                              oppositeUser.nickName,
                              style: KimmiTamperDaytime.white18,
                            ),
                            4.wGap,
                            oppositeUser.country == null
                                ? Container()
                                : Container(
                                    width: 20,
                                    height: 20,
                                    decoration: const BoxDecoration(
                                      shape: BoxShape.circle,
                                      color: Color(0xFFFFFFFF),
                                    ),
                                    alignment: Alignment.center,
                                    child: KimmiErnie.circle(
                                      url: oppositeUser.country!.flag ?? '',
                                      size: 18,
                                      clip: ImageClipType.origin,
                                      fit: BoxFit.cover,
                                    ),
                                  ),
                          ],
                        ),
                        8.hGap,
                        Row(
                          children: [
                            _kimmiObligationStature(oppositeUser),
                            _kimmiConcerned(oppositeUser),
                          ],
                        ),
                      ],
                    ),
              const Spacer(),
              Container(
                padding: const EdgeInsets.all(4),
                child: Transform.rotate(
                  angle: KimmiIOJuda.isARLanguage() ? pi : 0,
                  child: KimmiErnie.local(
                    fileName: 'kimmi_hombre_cradle_wit',
                    width: 12,
                    height: 12,
                  ),
                ),
              ),
            ],
          ),
          _kimmiTeaserFailed(oppositeUser.signature),
        ],
      ),
    ).click(() {
      logic.onKimmiApplauseSecurity(oppositeUser);
    });
  }

  Widget _kimmiObligationStature(KimmiWaitressFeast user) {
    if (user.age == null || user.age == 0) return Container();

    return Container(
      height: 24,
      padding: const EdgeInsets.symmetric(horizontal: 8),
      margin: const EdgeInsets.only(right: 8),
      decoration: BoxDecoration(
        color: "FF3396".toColor(opacity: 0.3),
        borderRadius: BorderRadius.circular(14),
      ),
      child: Row(
        mainAxisAlignment: MainAxisAlignment.start,
        children: [
          KimmiErnie.local(
            fileName: "kimmi_hombre_security_whimsical_color",
            width: 16,
            height: 16,
          ),
          4.wGap,
          Text("${user.age ?? 18}", style: KimmiTamperDaytime.white12Bold),
        ],
      ),
    );
  }

  Widget _kimmiConcerned(KimmiWaitressFeast user) {
    String? languages = user.languages;
    if (KimmiStarbucksJuda.isEmpty(languages)) return Container();

    List<KimmiStormConcerned>? showList = KIMMI.kimmiHump.getLanguagesByIds(
      languages!,
    );

    if (showList == null || showList.isEmpty) return Container();

    var list = showList.take(2).toList();

    return Row(
      children: List.generate(list.length, (index) {
        return Container(
          height: 24,
          padding: const EdgeInsets.symmetric(horizontal: 12),
          margin: const EdgeInsets.only(right: 4),
          decoration: const BoxDecoration(
            borderRadius: BorderRadius.all(Radius.circular(12)),
            color: KimmiDraftJuda.white_20p,
          ),
          child: Center(
            child: Text(
              list[index].name,
              style: KimmiTamperDaytime.white12,
              textAlign: TextAlign.center,
            ),
          ),
        );
      }),
    );
  }

  Widget _kimmiTeaserFailed(String? signature) {
    if (KimmiStarbucksJuda.isEmpty(signature) ||
        KIMMI.kimmiHump.isKimmiAiGraceSensitive()) {
      return Container(height: 8);
    }

    return Container(
      margin: const EdgeInsets.only(top: 12, bottom: 8),
      child: Text(
        "$signature",
        style: KimmiTamperDaytime.TextWhite_16_Medium,
        maxLines: 3,
        overflow: TextOverflow.ellipsis,
      ),
    );
  }
}
