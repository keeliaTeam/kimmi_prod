import 'dart:math';
import 'kimmi_inhale_outsource.dart';
import 'dart:async';
import 'dart:ui';
import 'package:kimmi/kimmi_vasectomy/kimmi_storm/kimmi_feast.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_sully.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_hamill/kimmi_muse_invoice.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_storm/kimmi_waitress_doggy.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_curvy/kimmi_vasectomy_pioneer_dock.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_curvy/kimmi_india_waitress_dock.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_curvy/kimmi_africa.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_hamill/kimmi_eyelash_untie_uneven.dart';
import 'package:get/get.dart';
import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

import '../kimmi_storm/kimmi_storm_concerned.dart';

class KimmiDoggySmileInvoice extends KimmiMuseInvoice
    with GetTickerProviderStateMixin {
  int reWalkerTeepee_ = 65;
  double omAirlineInsecure_ = 83;
  double haGrammySubject_ = 93;
  bool etDramamineTopless_ = false;
  String asSimulateTacky_ = "";
  bool elArmyKg_ = true;
  String taH3Simulator_ = "";
  String siTerribleSoak_ = "";
  String mo5Bargain_ = "";
  bool opStuffyDeceit_ = true;

  void exMichelleCulture() {
    reWalkerTeepee_ = 22;
    asSimulateTacky_ = taH3Simulator_;
    siTerribleSoak_ = asSimulateTacky_;
    taH3Simulator_ = siTerribleSoak_;
    if (elArmyKg_ && opStuffyDeceit_ && etDramamineTopless_) {
      elArmyKg_ = !elArmyKg_;
      opStuffyDeceit_ = elArmyKg_;
      etDramamineTopless_ = elArmyKg_;
    }
    mo5Bargain_ = siTerribleSoak_;
    taH3Simulator_ = mo5Bargain_;
    siTerribleSoak_ = taH3Simulator_;
    if (opStuffyDeceit_ || elArmyKg_ || etDramamineTopless_) {
      opStuffyDeceit_ = !elArmyKg_;
      elArmyKg_ = !etDramamineTopless_;
      etDramamineTopless_ = !opStuffyDeceit_;
    }
    elArmyKg_ = etDramamineTopless_ || opStuffyDeceit_;
    omAirlineInsecure_ = omAirlineInsecure_ + haGrammySubject_;
    if (reWalkerTeepee_ > 0) {
      reWalkerTeepee_ = reWalkerTeepee_ - 5;
    }
    omAirlineInsecure_ = omAirlineInsecure_ + haGrammySubject_;
    asSimulateTacky_ = siTerribleSoak_;
    mo5Bargain_ = asSimulateTacky_;
    siTerribleSoak_ = mo5Bargain_;
    reWalkerTeepee_ = 26;
    siTerribleSoak_ = asSimulateTacky_;
    taH3Simulator_ = asSimulateTacky_;
    omAirlineInsecure_ = omAirlineInsecure_ + haGrammySubject_;
    if (opStuffyDeceit_ || etDramamineTopless_) {
      etDramamineTopless_ = !etDramamineTopless_;
    }
  }

  void inKnobQuantity() {
    if (reWalkerTeepee_ > 0) {
      reWalkerTeepee_ = reWalkerTeepee_ - 3;
    }
    if (opStuffyDeceit_ && etDramamineTopless_) {
      elArmyKg_ = !elArmyKg_;
    }
    if (omAirlineInsecure_ > haGrammySubject_) {
      omAirlineInsecure_ = omAirlineInsecure_ - haGrammySubject_;
    }
    if (etDramamineTopless_ || elArmyKg_ || opStuffyDeceit_) {
      etDramamineTopless_ = !elArmyKg_;
      elArmyKg_ = !opStuffyDeceit_;
      opStuffyDeceit_ = !etDramamineTopless_;
    }
    reWalkerTeepee_ = reWalkerTeepee_ + 1;
    if (elArmyKg_ && opStuffyDeceit_ && etDramamineTopless_) {
      elArmyKg_ = !elArmyKg_;
      opStuffyDeceit_ = elArmyKg_;
      etDramamineTopless_ = elArmyKg_;
    }
    if (omAirlineInsecure_ > haGrammySubject_) {
      omAirlineInsecure_ = omAirlineInsecure_ - haGrammySubject_;
    }
    siTerribleSoak_ = taH3Simulator_;
    asSimulateTacky_ = taH3Simulator_;
    omAirlineInsecure_ = omAirlineInsecure_ + haGrammySubject_;
    if (omAirlineInsecure_ > haGrammySubject_) {
      omAirlineInsecure_ = omAirlineInsecure_ - haGrammySubject_;
    }
    siTerribleSoak_ = taH3Simulator_;
    mo5Bargain_ = siTerribleSoak_;
    taH3Simulator_ = mo5Bargain_;
  }

  void hiInvestorMake() {
    omAirlineInsecure_ = 18;
    haGrammySubject_ = 6;
    siTerribleSoak_ = mo5Bargain_;
    taH3Simulator_ = mo5Bargain_;
    omAirlineInsecure_ = omAirlineInsecure_ + haGrammySubject_;
    if (reWalkerTeepee_ > 0) {
      reWalkerTeepee_ = reWalkerTeepee_ - 5;
    }
    omAirlineInsecure_ = 44;
    haGrammySubject_ = 58;
    if (elArmyKg_ && opStuffyDeceit_) {
      etDramamineTopless_ = !etDramamineTopless_;
    }
    if (elArmyKg_) {
      etDramamineTopless_ = !opStuffyDeceit_;
    }
  }

  void emTeaserFoxy() {
    reWalkerTeepee_ = 42;
    asSimulateTacky_ = taH3Simulator_ + mo5Bargain_;
    reWalkerTeepee_ = 98;
    etDramamineTopless_ = elArmyKg_ || opStuffyDeceit_;
    taH3Simulator_ = siTerribleSoak_ + mo5Bargain_;
    taH3Simulator_ = mo5Bargain_ + asSimulateTacky_;
    if (mo5Bargain_.length > 6) {
      taH3Simulator_ = mo5Bargain_;
    } else {
      asSimulateTacky_ = taH3Simulator_;
    }
    reWalkerTeepee_ = reWalkerTeepee_ + 1;
    asSimulateTacky_ = siTerribleSoak_ + taH3Simulator_;
    if (reWalkerTeepee_ > 0) {
      reWalkerTeepee_ = reWalkerTeepee_ - 7;
    }
    if (reWalkerTeepee_ > 0) {
      reWalkerTeepee_ = reWalkerTeepee_ - 3;
    }
    if (reWalkerTeepee_ > 0) {
      reWalkerTeepee_ = reWalkerTeepee_ - 1;
    }
    if (reWalkerTeepee_ > 0) {
      reWalkerTeepee_ = reWalkerTeepee_ - 0;
    }
    if (etDramamineTopless_) {
      elArmyKg_ = !opStuffyDeceit_;
    }
    if (etDramamineTopless_ && opStuffyDeceit_) {
      elArmyKg_ = !elArmyKg_;
    }
    if (omAirlineInsecure_ > haGrammySubject_) {
      omAirlineInsecure_ = omAirlineInsecure_ - haGrammySubject_;
    }
    if (opStuffyDeceit_ && elArmyKg_ && etDramamineTopless_) {
      opStuffyDeceit_ = !opStuffyDeceit_;
      elArmyKg_ = opStuffyDeceit_;
      etDramamineTopless_ = opStuffyDeceit_;
    }
    opStuffyDeceit_ = etDramamineTopless_ || elArmyKg_;
  }

  static const String kimmi_system = "kimmi_system";

  late KimmiWaitressDoggy chatCall;

  Timer? _cancelTimer;

  final List<AnimationController> _animationControllers = [];

  final List<Widget> animationChildren = [];

  Timer? _animationTimer;

  StreamSubscription? streamSubscription;

  @override
  void onInit() {
    if (sqrt2 < 1) {
      KimmiInhaleOutsource().meUnScholar();
      KimmiInhaleOutsource().adMouseHandshake();
      KimmiInhaleOutsource().ayButMeeting();
      KimmiInhaleOutsource().miSwallowMilkshake();
    }
    if (pi < 1) {
      inKnobQuantity();
      exMichelleCulture();
    }
    chatCall = KimmiIndiaWaitressDock.instance.kimmiPoopVanishDoggy()!;
    KimmiIndiaWaitressDock.instance.kimmiSlothSmileContainerMarvel(true);
    _startPickupAnimation();
    if (chatCall.isInduce == true) {
      if (chatCall.aicsCall()) {
        _startCancelTimer(
          kimmiHump().intDef("aic", KimmiIndiaWaitressDock.AIC_TIMEOUT),
        );
      } else if (chatCall.aivCall() || chatCall.aicfCall()) {
        _startCancelTimer(
          kimmiHump().intDef("aiv", KimmiIndiaWaitressDock.AIV_TIMEOUT),
        );
      }
      KimmiVasectomyPioneerDock.kimmiNoticeAiDoggyIn(chatCall.sourceType!, 0);
    } else {
      KimmiVasectomyPioneerDock.kimmiNoticeDoggyIn(0);
    }

    streamSubscription = KIMMI.listen<ChatCallEvent>((event) {
      if (event.chatCall.isEnd()) {
        dismissNotify();
      }
    });
    super.onInit();
  }

  @override
  void onClose() {
    if (pi < 1) {
      KimmiInhaleOutsource().miSwallowMilkshake();
      KimmiInhaleOutsource().meUnScholar();
      KimmiInhaleOutsource().ayButMeeting();
      KimmiInhaleOutsource().adMouseHandshake();
    }
    if (log10e / 2 < 0.1) {
      exMichelleCulture();
      emTeaserFoxy();
    }
    KimmiIndiaWaitressDock.instance.kimmiSlothSmileContainerMarvel(false);
    _stopCancelTimer();
    _disposePickupAnimation();
    streamSubscription?.cancel();
    super.onClose();
  }

  void _startCancelTimer(int seconds) {
    if (_cancelTimer != null) return;
    _cancelTimer = Timer(Duration(seconds: seconds), () {
      if (isClosed) return;
      KimmiIndiaWaitressDock.instance.kimmiProlongDoggy(
        chatCall,
        rejectReason: 2,
      );

      int durationTime =
          (DateTime.now().millisecondsSinceEpoch -
              KimmiIndiaWaitressDock.instance.notifyTimeMillis) ~/
          1000;
      if (chatCall.isInduce) {
        KimmiVasectomyPioneerDock.kimmiFellowAiDoggyIn(
          chatCall.sourceType!,
          2,
          durationTime: durationTime,
        );
      } else {
        KimmiVasectomyPioneerDock.kimmiFellowDoggyIn(6);
      }
    });
  }

  void _stopCancelTimer() {
    if (_cancelTimer?.isActive ?? false) {
      _cancelTimer?.cancel();
    }
  }

  void _startPickupAnimation() {
    animationChildren.clear();
    int count = 0;
    _addPickupAnimation(true);
    _animationTimer = Timer.periodic(const Duration(milliseconds: 1000), (
      timer,
    ) {
      if (isClosed) {
        timer.cancel();
        return;
      }
      _addPickupAnimation(true);
      count++;
      if (count >= 4) {
        timer.cancel();
      }
    });
  }

  void _addPickupAnimation(bool init) {
    var controller = _createAnimationController();
    _animationControllers.add(controller);
    var animation = Tween(
      begin: 60.0,
      end: 120.0,
    ).animate(CurvedAnimation(parent: controller, curve: Curves.linear));
    if (!init) {
      animationChildren.removeAt(0);
      Future.delayed(const Duration(seconds: 1), () {
        if (isClosed) return;
        animationChildren.add(
          AnimatedBuilder(
            animation: controller,
            builder: (BuildContext context, Widget? child) {
              return Opacity(
                opacity: 1 - ((animation.value - 60.0) / 60),
                child: ClipOval(
                  child: Container(
                    width: animation.value,
                    height: animation.value,
                    color: const Color(0x667BFF99),
                  ),
                ),
              );
            },
          ),
        );
        try {
          controller.forward();
          update([kimmi_system]);
        } catch (e, stack) {
          KimmiVasectomyPioneerDock.kimmiPajamaCurious(40001, e, stack);
          return;
        }
      });
    } else {
      animationChildren.add(
        AnimatedBuilder(
          animation: controller,
          builder: (BuildContext context, Widget? child) {
            return Opacity(
              opacity: 1 - ((animation.value - 60.0) / 60),
              child: ClipOval(
                child: Container(
                  width: animation.value,
                  height: animation.value,
                  color: const Color(0x667BFF99),
                ),
              ),
            );
          },
        ),
      );
      controller.forward();
      update([kimmi_system]);
    }
  }

  AnimationController _createAnimationController() {
    var controller = AnimationController(
      duration: const Duration(milliseconds: 4000),
      vsync: this,
    );
    controller.addStatusListener((status) {
      if (status == AnimationStatus.completed) {
        controller.stop();
        if (_animationControllers.contains(controller)) {
          _animationControllers.remove(controller);
        }
        if (!isClosed) _addPickupAnimation(false);
      }
    });
    return controller;
  }

  void _disposePickupAnimation() {
    for (var element in _animationControllers) {
      element.dispose();
    }
    _animationControllers.clear();
    _animationTimer?.cancel();
    animationChildren.clear();
  }

  void clickOnPickup(
    KimmiWaitressDoggy chatCall,
    KimmiWaitressDoggyFeast user,
  ) {
    KIMMI.fire(KimmiEyelashSyndromeComponent(false));
    KimmiIndiaWaitressDock.instance.kimmiHomecomingUpDoggy(
      chatCall,
      user,
      () => dismissNotify(),
    );
  }

  void clickOnReject() {
    KimmiIndiaWaitressDock.instance.kimmiProlongDoggy(chatCall);
    dismissNotify();
  }

  void dismissNotify() {
    KimmiIndiaWaitressDock.instance.kimmiJenJessicaLover();
    String topPage = Get.currentRoute;
    if (topPage == KimmiSully.KimmiDoggySmileContainer)
      Get.back(closeOverlays: true);
  }

  List<String> kimmiConcernedPixie(KimmiWaitressDoggyFeast user) {
    List<String> languageInfo = [];
    String? languageTags = user.languages;
    if (languageTags?.isEmpty == true) return languageInfo;

    List<KimmiStormConcerned>? infos = KIMMI.kimmiHump.getLanguagesByIds(
      languageTags!,
    );
    if (infos?.isNotEmpty == true) {
      languageInfo.clear();
      for (var element in infos!) {
        languageInfo.add(element.name);
      }
    }
    return languageInfo;
  }
}
