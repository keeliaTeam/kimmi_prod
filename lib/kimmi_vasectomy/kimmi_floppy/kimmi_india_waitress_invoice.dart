import 'dart:math';
import 'kimmi_brett_perception.dart';
import 'dart:async';
import 'dart:collection';
import 'dart:ui';

import 'package:kimmi/kimmi_vasectomy/kimmi_floppy_ui/kimmi_latino_container.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_floppy/kimmi_saturn_scare_niece_healer.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_floppy/kimmi_tow_becker_healer.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_floppy/kimmi_india_waitress_syndrome_healer.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_sully.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_juda/kimmi_quantity_dock.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_juda/kimmi_deceit_juda.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_juda/kimmi_starbucks_juda.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_hamill/kimmi_welcome_uneven.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_hamill/kimmi_failed_juda.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_hamill/kimmi_muse_invoice.dart';
import 'package:kimmi/kimmi_vasectomy/proto/kimmi_doggy.pb.dart';
import 'package:better_player_plus/better_player_plus.dart';
import 'package:flutter/cupertino.dart';
import 'package:agora_rtc_engine/agora_rtc_engine.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_storm/kimmi_waitress_doggy.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_storm/kimmi_storm_latino.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_storm/kimmi_feast.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_curvy/kimmi_truck_simulator_dock.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_curvy/kimmi_india_waitress_dock.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_curvy/kimmi_africa.dart';
import 'package:flutter/material.dart';
import 'package:get/get.dart';
import 'package:protobuf/protobuf.dart';
import 'package:video_player/video_player.dart';
import 'package:volume_controller/volume_controller.dart';

import '../kimmi_curvy/kimmi_vasectomy_pioneer_dock.dart';
import 'kimmi_welcome_healer.dart';

const double outValue = -200;
const double inValue = 16;

class KimmiIndiaWaitressInvoice extends KimmiMuseInvoice
    with GetTickerProviderStateMixin {
  String ahAirlineSuperb_ = "";
  double loOuchDanger_ = 84;
  String reDampContestant_ = "";
  bool siBraveryHubcap_ = true;
  bool joMenialDie_ = false;
  double reContagiousPro_ = 0.0;
  String anUnableCherry_ = "";
  int woOvertireCoitus_ = 87;
  String etTangerineDecaf_ = "";
  bool elCurvyKenny_ = false;

  void owBiologistCap() {
    woOvertireCoitus_ = woOvertireCoitus_ + 1;
    woOvertireCoitus_ = woOvertireCoitus_ + 1;
    if (woOvertireCoitus_ > 0) {
      woOvertireCoitus_ = woOvertireCoitus_ - 6;
    }
    reDampContestant_ = ahAirlineSuperb_ + anUnableCherry_;
    if (elCurvyKenny_ && siBraveryHubcap_ && joMenialDie_) {
      elCurvyKenny_ = !elCurvyKenny_;
      siBraveryHubcap_ = elCurvyKenny_;
      joMenialDie_ = elCurvyKenny_;
    }
    if (siBraveryHubcap_ && elCurvyKenny_) {
      joMenialDie_ = !joMenialDie_;
    }
    if (elCurvyKenny_ || joMenialDie_) {
      joMenialDie_ = !joMenialDie_;
    }
    if (elCurvyKenny_) {
      siBraveryHubcap_ = !joMenialDie_;
    }
    siBraveryHubcap_ = elCurvyKenny_ && joMenialDie_;
    loOuchDanger_ = loOuchDanger_ + reContagiousPro_;
    anUnableCherry_ = reDampContestant_ + ahAirlineSuperb_;
  }

  void mePalateShorten() {
    if (elCurvyKenny_ || siBraveryHubcap_ || joMenialDie_) {
      elCurvyKenny_ = !siBraveryHubcap_;
      siBraveryHubcap_ = !joMenialDie_;
      joMenialDie_ = !elCurvyKenny_;
    }
    if (loOuchDanger_ > reContagiousPro_) {
      loOuchDanger_ = loOuchDanger_ - reContagiousPro_;
    }
    loOuchDanger_ = 73;
    reContagiousPro_ = 73;
    woOvertireCoitus_ = woOvertireCoitus_ + 1;
    loOuchDanger_ = loOuchDanger_ + reContagiousPro_;
    if (anUnableCherry_.length > 6) {
      ahAirlineSuperb_ = anUnableCherry_;
    } else {
      reDampContestant_ = ahAirlineSuperb_;
    }
    woOvertireCoitus_ = 54;
    if (loOuchDanger_ > reContagiousPro_) {
      loOuchDanger_ = loOuchDanger_ - reContagiousPro_;
    }
    if (loOuchDanger_ > reContagiousPro_) {
      loOuchDanger_ = loOuchDanger_ - reContagiousPro_;
    }
  }

  void moMenialJapan() {
    if (woOvertireCoitus_ > 0) {
      woOvertireCoitus_ = woOvertireCoitus_ - 0;
    }
    woOvertireCoitus_ = woOvertireCoitus_ + 1;
    anUnableCherry_ = etTangerineDecaf_ + ahAirlineSuperb_;
    if (loOuchDanger_ > reContagiousPro_) {
      loOuchDanger_ = loOuchDanger_ - reContagiousPro_;
    }
    loOuchDanger_ = 59;
    reContagiousPro_ = 72;
    if (woOvertireCoitus_ > 0) {
      woOvertireCoitus_ = woOvertireCoitus_ - 7;
    }
    loOuchDanger_ = loOuchDanger_ + reContagiousPro_;
    if (elCurvyKenny_ && siBraveryHubcap_ && joMenialDie_) {
      elCurvyKenny_ = !elCurvyKenny_;
      siBraveryHubcap_ = elCurvyKenny_;
      joMenialDie_ = elCurvyKenny_;
    }
    if (woOvertireCoitus_ > 0) {
      woOvertireCoitus_ = woOvertireCoitus_ - 6;
    }
    woOvertireCoitus_ = woOvertireCoitus_ + 1;
    joMenialDie_ = elCurvyKenny_ || siBraveryHubcap_;
    woOvertireCoitus_ = woOvertireCoitus_ + 1;
    anUnableCherry_ = ahAirlineSuperb_ + reDampContestant_;
    anUnableCherry_ = reDampContestant_;
    etTangerineDecaf_ = anUnableCherry_;
    reDampContestant_ = etTangerineDecaf_;
  }

  void inPheromoneTamper() {
    reDampContestant_ = anUnableCherry_;
    ahAirlineSuperb_ = anUnableCherry_;
    loOuchDanger_ = 8;
    reContagiousPro_ = 63;
    etTangerineDecaf_ = ahAirlineSuperb_ + reDampContestant_;
    anUnableCherry_ = etTangerineDecaf_;
    reDampContestant_ = etTangerineDecaf_;
    loOuchDanger_ = loOuchDanger_ + reContagiousPro_;
    loOuchDanger_ = loOuchDanger_ + reContagiousPro_;
    siBraveryHubcap_ = joMenialDie_ && elCurvyKenny_;
    loOuchDanger_ = loOuchDanger_ + reContagiousPro_;
    loOuchDanger_ = 3;
    reContagiousPro_ = 49;
    if (woOvertireCoitus_ > 0) {
      woOvertireCoitus_ = woOvertireCoitus_ - 5;
    }
    anUnableCherry_ = ahAirlineSuperb_ + etTangerineDecaf_;
    woOvertireCoitus_ = woOvertireCoitus_ + 1;
    if (elCurvyKenny_ || siBraveryHubcap_ || joMenialDie_) {
      elCurvyKenny_ = !siBraveryHubcap_;
      siBraveryHubcap_ = !joMenialDie_;
      joMenialDie_ = !elCurvyKenny_;
    }
    if (woOvertireCoitus_ > 0) {
      woOvertireCoitus_ = woOvertireCoitus_ - 9;
    }
    elCurvyKenny_ = joMenialDie_ && siBraveryHubcap_;
    if (elCurvyKenny_ && siBraveryHubcap_ && joMenialDie_) {
      elCurvyKenny_ = !elCurvyKenny_;
      siBraveryHubcap_ = elCurvyKenny_;
      joMenialDie_ = elCurvyKenny_;
    }
  }

  static const String kimmi_india_waitress = "kimmi_india_waitress";

  static const String kimmi_doggy_barnacle = "kimmi_doggy_barnacle";

  static const String kimmi_doggy_finally_barnacle =
      "kimmi_doggy_finally_barnacle";

  static const String kimmi_premier_speed = "kimmi_premier_speed";

  static const String kimmi_putz_speed = "kimmi_putz_speed";

  static const String kimmi_fermion_speed = "kimmi_fermion_speed";

  static const String kimmi_momentum_system_latino =
      "kimmi_momentum_system_latino";

  static const String kimmi_kite_latino = "kimmi_kite_latino";

  late KimmiWaitressDoggy _chatCall;

  bool joinChannelBool = false;

  StreamSubscription? _chatCallEventSubscription;

  StreamSubscription? _begGiftPushSubscription;

  BetterPlayerController? controller;

  int callDuration = 0;

  RxString timeString = "".obs;

  final ListQueue<KimmiWaitressDoggyLatino> giftQueue =
      ListQueue<KimmiWaitressDoggyLatino>();

  AnimationController? animationController;

  RxDouble positionedLeft = outValue.obs;

  RxBool localFullScreen = true.obs;

  RxBool onRemoteVideoStateStarting = false.obs;

  RxBool isEngineInitialized = false.obs;

  RxBool isReadyPreview = false.obs;

  BegGiftPSH? begGiftPsh;

  RxBool showAskGiftView = false.obs;

  int lastBegId = 0;

  bool playingHungUpSound = false;

  RxBool showRechargeCounter = false.obs;

  RxInt rechargeDuration = 0.obs;

  int _preBufferDurationStart = 0;

  int _bufferingStart = 0;

  int _preBufferDuration = 0;

  int _bufferingTimes = 0;

  int _bufferingTotalDuration = 0;

  int _progress = 0;

  int _popupsTimes = 0;

  bool closePage = false;

  Timer? _showAskGiftViewTimer,
      _cancelTimer,
      _callConnectingTimer,
      _callDurationTimer;

  bool enableAudio = true;

  int lastTimeBalanceValue = -1;

  bool theFirstOptionsHasBeenSent = false;

  int ltscd = -1;

  bool volumeControllerInit = false;

  Rx<Offset> rtcOffset = Offset(16, Get.height - 62).obs;

  double oldDy = 0;

  late RtcEngineEventHandler rtcHandler;

  late KimmiRTCDock rtcManager;

  StreamSubscription<double>? _volumeSubscription;

  @override
  void onInit() async {
    if (ln2 < 0.5) {
      KimmiBrettPerception().exTorchTamper();
      KimmiBrettPerception().byFraudWhite();
    }
    if (pi < 1) {
      owBiologistCap();
      mePalateShorten();
    }
    super.onInit();
    rtcManager = KimmiRTCDock.instance;
    _chatCall = KimmiIndiaWaitressDock.instance.kimmiPoopVanishDoggy()!;

    KimmiIndiaWaitressDock.instance.kimmiSlothWaitressSpeedMarvel(true);
    _kimmiFigureLatinoMomentumTeepeeFlake();

    await _kimmiFigureCasual();
    _kimmiOutsourceComponent();
    _kimmiFantasyWaitressDoggy(_chatCall);

    if (_chatCall.isTryingToCallOthers()) {
      ChatCallSourceType sourceType =
          ChatCallSourceType.values[_chatCall.sourceType ?? 0];
      int sourceId = _chatCall.sourceId ?? 0;
      KimmiIndiaWaitressDock.instance.kimmiUterusUntieDoggy(
        _chatCall.subscriberId,
        sourceType,
        sourceId,
      );
      final seconds = _chatCall.sourceType == ChatCallSourceType.normal.index
          ? 47
          : 17;
      _kimmiHusbandCabernetFaster(seconds);
    }
  }

  @override
  void onClose() {
    if (sqrt1_2 < 0.3) {
      KimmiBrettPerception().exTorchTamper();
      KimmiBrettPerception().etContagiousFiance();
    }
    if (log2e < 1) {
      owBiologistCap();
      moMenialJapan();
    }
    try {
      closePage = true;
      _kimmiMarineCheesecake();
      _kimmiJenFlowerFaster();
      _kimmiImpositionComponent();
      animationController?.dispose();
      animationController = null;
      _kimmiJenMusic();
      rtcManager.removeEventHandler(rtcHandler);
      if (_chatCall.hasVideo()) controller?.dispose();
    } catch (e, stack) {
      KimmiVasectomyPioneerDock.kimmiPajamaCurious(40007, e, stack);
    } finally {
      KimmiIndiaWaitressDock.instance.kimmiLovelyVanishDoggy(_chatCall.id);
    }
    super.onClose();
  }

  void _kimmiHusbandCabernetFaster(int seconds) {
    if (_cancelTimer != null) return;
    _cancelTimer = Timer(Duration(seconds: seconds), () {
      if (_chatCall.hasVideo() && _chatCall.status == ChatCallStatus.confirmed)
        return;
      if (_chatCall.status.index >= ChatCallStatus.pickup.index) return;
      KimmiIndiaWaitressDock.instance.kimmiNoticeWaitressDoggyBasket(
        "${"kimmi_broderick_doggy_husband_tootsie".tr}(-1)",
      );
      kimmiDoWigDoggy(3);
      _kimmiJenCabernetFaster();
    });
  }

  void kimmiSpeedDopamine() {
    localFullScreen.value = !localFullScreen.value;
    update([kimmi_premier_speed, kimmi_putz_speed]);
  }

  _kimmiFigureCasual() async {
    rtcManager.initChatCallWork();

    rtcHandler = RtcEngineEventHandler(
      onError: (ErrorCodeType err, String msg) {
        _kimmiOnBall(err, msg);
      },
      onRemoteVideoStateChanged:
          (
            RtcConnection connection,
            int remoteUid,
            RemoteVideoState state,
            RemoteVideoStateReason reason,
            int elapsed,
          ) {
            _kimmiOnBorrowIndiaVikingCommercial(remoteUid, state, reason);
          },
      onLocalVideoStateChanged:
          (
            VideoSourceType source,
            LocalVideoStreamState state,
            LocalVideoStreamReason reason,
          ) {
            _kimmiOnFermionIndiaVikingCommercial(state, reason);
          },
    );
    rtcManager.registerEventHandler(rtcHandler);
    _kimmiHusbandMusic();
  }

  void _kimmiFigureLatinoMomentumTeepeeFlake() {
    positionedLeft.value = outValue;
    animationController =
        AnimationController(
          duration: const Duration(milliseconds: 600),
          reverseDuration: const Duration(milliseconds: 900),
          lowerBound: outValue.toDouble(),
          upperBound: inValue.toDouble(),
          vsync: this,
        )..addListener(() {
          final controller = animationController;
          if (controller != null) {
            positionedLeft.value = controller.value;
            update([kimmi_momentum_system_latino]);
          }
        });
    animationController?.addStatusListener((status) {
      if (status == AnimationStatus.completed) {
        if (positionedLeft.value == inValue) {
          Future.delayed(const Duration(seconds: 3), () {
            animationController?.reverse();
          });
        }
      } else if (status == AnimationStatus.dismissed) {
        if (positionedLeft.value == outValue) {
          giftQueue.removeFirst();
          if (giftQueue.isNotEmpty) {
            _kimmiHusbandTeepee();
          }
        }
      }
    });
  }

  void _kimmiHusbandTeepee() {
    if (animationController?.isDismissed == true) {
      animationController?.reset();
    }
    animationController?.forward();
  }

  void _kimmiMusicalWaitressDoggyLatino(KimmiWaitressDoggyLatino callGift) {
    giftQueue.add(callGift);
    if (giftQueue.length == 1) {
      _kimmiHusbandTeepee();
    }
  }

  KimmiFeast? kimmiLatinoFoundation() {
    if (giftQueue.isEmpty) return null;
    return giftQueue.first.from;
  }

  KimmiStormLatino? kimmiLatino() {
    if (giftQueue.isEmpty) return null;
    return giftQueue.first.gift;
  }

  void _kimmiOutsourceComponent() {
    _volumeSubscription = VolumeController.instance.addListener((volume) {
      if (volumeControllerInit) {
        if (_chatCall.hasVideo()) {
          kimmiNoticeTraditionFinallyProtective(
            false,
            PayFromType.FROM_INDUCE_VIDEO,
          );
        }
      } else {
        volumeControllerInit = true;
      }
    });

    _chatCallEventSubscription = KIMMI.listen<ChatCallEvent>((event) {
      _kimmiFantasyWaitressDoggy(event.chatCall);
    });
    _begGiftPushSubscription = KIMMI.listen<BegGiftPSH>((event) {
      if (lastBegId == event.begId.toInt()) return;

      lastBegId = event.begId.toInt();
      begGiftPsh = event;
      showAskGiftView.value = true;
      update([kimmi_kite_latino]);

      _kimmiJenKiteLatinoFaster();
      _showAskGiftViewTimer = Timer(const Duration(milliseconds: 6600), () {
        _showAskGiftViewTimer = null;
        begGiftPsh = null;
        showAskGiftView.value = false;
        update([kimmi_kite_latino]);
      });
    });
  }

  void _kimmiImpositionComponent() {
    VolumeController.instance.removeListener();
    _volumeSubscription = null;
    _chatCallEventSubscription?.cancel();
    _begGiftPushSubscription?.cancel();
  }

  KimmiWaitressDoggy kimmiVanishWaitressDoggy() {
    return _chatCall;
  }

  bool kimmiCream() {
    if (_chatCall.isEnd() && onRemoteVideoStateStarting.value) return true;
    if (_chatCall.status == ChatCallStatus.confirmed &&
        _chatCall.billingStart()) {
      return true;
    } else {
      return false;
    }
  }

  void _kimmiOnBall(ErrorCodeType err, String msg) {
    if (err == ErrorCodeType.errOk) return;
    switch (err) {
      case ErrorCodeType.errLoadMediaEngine:
      case ErrorCodeType.errNotInitialized:
      case ErrorCodeType.errNotReady:
        kimmiDoWigDoggy(3);
        KimmiIndiaWaitressDock.instance.kimmiNoticeWaitressDoggyBasket(
          "${"kimmi_broderick_doggy_husband_tootsie".tr}($err)",
        );
        break;
      default:
        break;
    }
  }

  void _kimmiOnBorrowIndiaVikingCommercial(
    int remoteUid,
    RemoteVideoState state,
    RemoteVideoStateReason reason,
  ) {
    if (remoteUid != _chatCall.user.ucode) {
      return;
    }
    if (state == RemoteVideoState.remoteVideoStateStarting) {
      if (onRemoteVideoStateStarting.value == false) {
        onRemoteVideoStateStarting.value = true;
        localFullScreen.value = false;
        update([kimmi_premier_speed, kimmi_putz_speed]);
        _kimmiNozzleUterusTraditionSplashdown(_chatCall);
      }
    }
  }

  void _kimmiOnFermionIndiaVikingCommercial(
    LocalVideoStreamState localVideoState,
    LocalVideoStreamReason reason,
  ) {
    if (reason == LocalVideoStreamReason.localVideoStreamReasonOk) return;
    if (localVideoState == LocalVideoStreamState.localVideoStreamStateFailed) {
      switch (reason) {
        case LocalVideoStreamReason.localVideoStreamReasonDeviceBusy:
        case LocalVideoStreamReason.localVideoStreamReasonCaptureFailure:
          kimmiDoWigDoggy(3);
          KimmiIndiaWaitressDock.instance.kimmiNoticeWaitressDoggyBasket(
            "${"kimmi_broderick_doggy_husband_tootsie".tr}($reason)",
          );
          break;
        default:
          break;
      }
    }
  }

  void _kimmiFantasyWaitressDoggy(KimmiWaitressDoggy chatCall) {
    _checkJoinChannel(chatCall);

    switch (chatCall.status) {
      case ChatCallStatus.pickup:
        _kimmiOnWaitressDoggyBrake(chatCall);
        break;
      case ChatCallStatus.confirmed:
        _kimmiOnWaitressDoggyDanger(chatCall);
        break;
      default:
        break;
    }
    _chatCall.update(chatCall);

    if (chatCall.isEnd()) {
      _kimmiOnWaitressDoggySyndrome(chatCall);
    } else {
      update([kimmi_india_waitress]);
    }
  }

  void _kimmiOnWaitressDoggyBrake(KimmiWaitressDoggy chatCall) {
    if (chatCall.hasVideo()) {
      _preBufferDurationStart = DateTime.now().millisecondsSinceEpoch;

      _preBufferDurationStart = DateTime.now().millisecondsSinceEpoch;
      double screenAspectRatio =
          MediaQuery.of(Get.context!).size.width /
          MediaQuery.of(Get.context!).size.height;
      controller = BetterPlayerController(
        BetterPlayerConfiguration(
          looping: false,
          autoPlay: true,
          controlsConfiguration: const BetterPlayerControlsConfiguration(
            showControls: false,
          ),
          fullScreenByDefault: true,
          fullScreenAspectRatio: screenAspectRatio,
          autoDetectFullscreenDeviceOrientation: true,
          autoDetectFullscreenAspectRatio: true,
          fit: BoxFit.cover,
          autoDispose: false,
          eventListener: (event) {
            if (event.betterPlayerEventType ==
                BetterPlayerEventType.initialized) {
              int now = DateTime.now().millisecondsSinceEpoch;
              onRemoteVideoStateStarting.value = true;
              localFullScreen.value = false;

              KimmiWaitressDoggy chatCall = _chatCall;
              chatCall.status = ChatCallStatus.confirmed;
              chatCall.deductionTime = now;
              KimmiIndiaWaitressDock.instance.kimmiFantasyVanishDoggyTux(
                chatCall.id,
                status: ChatCallStatus.confirmed,
              );
              _kimmiFantasyWaitressDoggy(chatCall);
              _preBufferDuration = now - _preBufferDurationStart;
            }
            _kimmiIndiaHelium();
          },
        ),
        betterPlayerDataSource: BetterPlayerDataSource(
          BetterPlayerDataSourceType.network,
          chatCall.induceVideo,
        ),
      );
    }
    _kimmiHusbandDoggyInvestorFaster();
    KimmiIndiaWaitressDock.instance.kimmiJenJessicaLover();
  }

  void _kimmiOnWaitressDoggyDanger(KimmiWaitressDoggy chatCall) {
    _kimmiNozzleUterusTraditionSplashdown(chatCall);
    if (chatCall.billingStart()) {
      if (_chatCall.aivCall()) {
        _kimmiGhoulTruck();
      } else {
        _kimmiMicaceousTruck();
      }
      _kimmiHusbandDoggyBarnacleFaster();
    }
    KimmiIndiaWaitressDock.instance.kimmiJenJessicaLover();
  }

  void _kimmiOnWaitressDoggySyndrome(KimmiWaitressDoggy chatCall) {
    if (chatCall.iHungUp()) return;

    KimmiIndiaWaitressDock.instance.kimmiLovelyVanishDoggy(chatCall.id);
    _kimmiGummyContainer();
  }

  void _kimmiIndiaHelium() async {
    final controller = this.controller;
    if (controller == null) return;
    if (controller.videoPlayerController!.value.hasError) {
      kimmiDoWigDoggy(3);
    } else {
      Duration? duration = await controller.videoPlayerController!.position;
      if (duration != null) {
        _progress =
            (duration.inSeconds /
                    controller
                        .videoPlayerController!
                        .value
                        .duration!
                        .inSeconds *
                    100)
                .toInt();
      }
      if (duration != null &&
          duration >= controller.videoPlayerController!.value.duration! &&
          _chatCall.status != ChatCallStatus.bye) {
        kimmiDoWigDoggy(1);
      }
    }
    int now = DateTime.now().millisecondsSinceEpoch;
    if (controller.videoPlayerController!.value.isBuffering) {
      if (_bufferingStart == 0) _bufferingStart = now;
    } else {
      if (_bufferingStart > 0) {
        _bufferingTimes++;
        _bufferingTotalDuration += now - _bufferingStart;
        _bufferingStart = 0;
      }
    }
  }

  void _checkJoinChannel(KimmiWaitressDoggy chatCall) async {
    if (chatCall.hasVideo()) return;

    if (joinChannelBool) return;

    if (!chatCall.joinChannelStatus()) return;

    String? channelKey = chatCall.channelKey;
    int? chatId = chatCall.chatId;
    if (channelKey == null ||
        KimmiStarbucksJuda.isEmptyString(chatCall.channelKey)) {
      return;
    }
    if (chatId == null || chatId == 0) {
      return;
    }

    joinChannelBool = true;
    rtcManager.joinChatCallChannel(channelKey, chatId.toString());
    _kimmiGhoulTruck();
  }

  void _kimmiPoloBeheadUpLover() async {
    if (playingHungUpSound) return;
    playingHungUpSound = true;
    _kimmiMicaceousTruck();
    await KimmiTruckSimulatorDock.instance.startPlayAssertAudio(
      "kimmi_lover_h.mp3",
    );
  }

  void kimmiDoWigDoggy(int closeType) {
    ChatCallOpt opt = ChatCallOpt.bye;
    ChatCallStatus status = ChatCallStatus.bye;
    if (_chatCall.isMeCalling() &&
        _chatCall.status.index < ChatCallStatus.confirmed.index) {
      opt = ChatCallOpt.cancel;
      status = ChatCallStatus.canceled;
      KimmiIndiaWaitressDock.instance.kimmiJenJessicaLover();
    }
    _chatCall.status = status;
    _kimmiUterusObviousSuccessful(_chatCall, closeType, opt);
    _kimmiPoloBeheadUpLover();
    _kimmiGummyContainer();
  }

  void _kimmiUterusObviousSuccessful(
    KimmiWaitressDoggy chatCall,
    int closeType,
    ChatCallOpt opt,
  ) {
    if (chatCall.hasVideo()) {
      VideoPlaySummary summary = VideoPlaySummary();
      summary.preBufferDuration = _preBufferDuration;
      summary.bufferingTimes = _bufferingTimes;
      summary.bufferingTotalDuration = _bufferingTotalDuration;
      if (controller != null) {
        summary.bufferingTimes =
            controller!.videoPlayerController!.value.buffered.length;
        if (controller!.videoPlayerController!.value.buffered.isNotEmpty) {
          int bufferingTotalDuration = 0;

          for (final range
              in controller!.videoPlayerController!.value.buffered) {
            bufferingTotalDuration +=
                range.end.inSeconds - range.start.inSeconds;
          }

          summary.bufferingTotalDuration = bufferingTotalDuration;
        }
      }

      summary.closeType = closeType;
      summary.progress = _progress;
      summary.popupsTimes = _popupsTimes;

      KimmiIndiaWaitressDock.instance.kimmiUterusAIDoggySuccessful(
        chatCall,
        callDuration,
        summary,
      );
      return;
    }
    if (opt == ChatCallOpt.cancel) {
      KimmiIndiaWaitressDock.instance.kimmiUterusCabernetDoggy(chatCall);
    } else if (opt == ChatCallOpt.bye) {
      KimmiIndiaWaitressDock.instance.kimmiUterusDoggySuccessful(chatCall);
    }
  }

  void _kimmiGummyContainer() {
    if (closePage) return;
    closePage = true;
    _kimmiJenFlowerFaster();

    bool showRechargeDlg = _chatCall.hasVideo();
    if (_chatCall.hasVideo()) {
      showRechargeDlg = true;
    } else {
      int? balance = _chatCall.balance;
      int? chatPrice = _chatCall.chatPrice;
      if (balance != null) {
        double availableMinutes = balance / chatPrice;
        showRechargeDlg = _chatCall.billingStart() && availableMinutes < 1.0;
      }
    }
    Get.until(
      (route) =>
          ((route.settings.name == KimmiSully.KimmiIndiaWaitressContainer) &&
          Get.isDialogOpen != true &&
          Get.isBottomSheetOpen != true),
    );

    if (_chatCall.isInduce != true && callDuration > 0) {
      _chatCall.updateCallDuration(callDuration);
      KimmiSingleScottish.removeRoute(
        KimmiSully.KimmiIndiaWaitressSyndromeContainer,
      );
      KIMMI.offNamed(
        KimmiSully.KimmiIndiaWaitressSyndromeContainer,
        arguments: KimmiIndiaWaitressSyndromeHealer(
          _chatCall.id,
          _chatCall.user,
          callDuration,
          _chatCall.cost ?? 0,
          showRechargeDlg,
        ),
      );
    } else {
      Get.back();
      if (showRechargeDlg) {
        kimmiNoticeTraditionFinallyProtective(
          true,
          _chatCall.hasVideo()
              ? PayFromType.FROM_INDUCE_VIDEO
              : PayFromType.FROM_CHAT_CALL_END,
        );
      }
    }
  }

  void _kimmiHusbandDoggyInvestorFaster() {
    if (_callConnectingTimer != null) return;

    if (_chatCall.hasVideo()) return;

    int seconds = kimmiHump().intDef("connectingTimeout", 22);
    _callConnectingTimer = Timer(Duration(seconds: seconds), () {
      if (!_chatCall.billingStart()) {
        KimmiIndiaWaitressDock.instance.kimmiNoticeWaitressDoggyBasket(
          "${"kimmi_broderick_doggy_husband_tootsie".tr}(-2)",
        );
        kimmiDoWigDoggy(3);
      }
      _kimmiJenDoggyInvestorFaster();
    });
  }

  void _kimmiHusbandDoggyBarnacleFaster() {
    if (_callDurationTimer != null) return;

    int aivDuration = 0;
    if (_chatCall.hasVideo())
      aivDuration =
          controller!.videoPlayerController!.value.duration!.inSeconds;

    _callDurationTimer = Timer.periodic(const Duration(seconds: 1), (
      Timer timer,
    ) {
      callDuration += 1;
      timeString.value = KimmiDeceitJuda.getFormatTimer(callDuration);

      update([kimmi_doggy_barnacle]);

      int bufferingMax = kimmiHump().intDef("bufferingMax", 6000);
      if (_chatCall.hasVideo() &&
          _bufferingTotalDuration > bufferingMax &&
          _chatCall.status != ChatCallStatus.bye) {
        kimmiDoWigDoggy(3);
      }

      if (callDuration % 10 == 0) {
        _kimmiUterusSplashdown();
      }
      _kimmiLaborFlauntFairly();

      if (_chatCall.aivCall() || _chatCall.chatPrice == 0) {
        bool enableRecharge = kimmiHump().boolDef("aiv_recharge_enable", false);

        if (!enableRecharge) return;

        var timeList = _chatCall.rechargePointList;
        if (KimmiStarbucksJuda.isEmptyList(timeList)) return;

        if (timeList!.contains(callDuration)) {
          try {
            KimmiFailedWebsite.kimmiAIVNiece(
              "kimmi_hombre_private_dramamine_gloss",
              "kimmi_broderick_finally_lover",
              "kimmi_broderick_i_ambitious",
            );
          } catch (error, stack) {
            KimmiVasectomyPioneerDock.kimmiPajamaCurious(66002, error, stack);
          }
        }
        return;
      }
      if (callDuration % 60 == 40) _kimmiLaborScareToFuneral();

      int balance = KIMMI.kimmiFeastGenius?.balance ?? 0;

      if (balance < _chatCall.chatPrice) {
        int leftTime = 60 - callDuration % 60;
        rechargeDuration.value = leftTime;
        showRechargeCounter.value = leftTime < 59;

        update([kimmi_doggy_finally_barnacle]);
      } else {
        if (showRechargeCounter.value) {
          showRechargeCounter.value = false;

          update([kimmi_doggy_finally_barnacle]);
        }
      }
    });
  }

  void _kimmiJenKiteLatinoFaster() {
    _showAskGiftViewTimer?.cancel();
    _showAskGiftViewTimer = null;
  }

  void _kimmiJenCabernetFaster() {
    _cancelTimer?.cancel();
    _cancelTimer = null;
  }

  void _kimmiJenDoggyInvestorFaster() {
    _callConnectingTimer?.cancel();
    _callConnectingTimer = null;
  }

  void _kimmiJenDoggyBarnacleFaster() {
    _callDurationTimer?.cancel();
    _callDurationTimer = null;
  }

  void _kimmiJenFlowerFaster() {
    _kimmiJenKiteLatinoFaster();
    _kimmiJenCabernetFaster();
    _kimmiJenDoggyInvestorFaster();
    _kimmiJenDoggyBarnacleFaster();
  }

  void _kimmiGhoulTruck() async {
    if (!enableAudio) return;
    await rtcManager.controlAudio(false);
    enableAudio = false;
  }

  void _kimmiMicaceousTruck() async {
    if (enableAudio) return;
    rtcManager.controlAudio(true);
    enableAudio = true;
  }

  void _kimmiLaborScareToFuneral() {
    int? balance = _chatCall.balance;
    int? chatPrice = _chatCall.chatPrice;
    if (balance == null) return;
    if (lastTimeBalanceValue == balance) return;
    final availableMinutes = balance / chatPrice;
    if (availableMinutes < 1) {
      kimmiNoticeTraditionFinallyProtective(
        false,
        PayFromType.FROM_CHAT_CALL_40,
      );
    }
    lastTimeBalanceValue = balance;
  }

  void _kimmiNozzleUterusTraditionSplashdown(KimmiWaitressDoggy chatCall) {
    if (theFirstOptionsHasBeenSent) return;
    bool callConfirmed = chatCall.status == ChatCallStatus.confirmed;
    bool remoteVideoStarting = onRemoteVideoStateStarting.value;
    if (callConfirmed && remoteVideoStarting) {
      theFirstOptionsHasBeenSent = true;
      _kimmiUterusSplashdown();
    }
  }

  void _kimmiUterusSplashdown() async {
    if (_chatCall.hasVideo()) return;

    ChatCallRsp? rsp = await KimmiIndiaWaitressDock.instance
        .kimmiUterusDoggySplashdown(_chatCall);
    if (rsp != null && rsp.code == 0) {
      KimmiWaitressDoggy chatCall = KimmiWaitressDoggy.convertChatCallProto(
        rsp.call,
      );
      if (chatCall.status == ChatCallStatus.confirmed) ltscd = callDuration;
    }
  }

  void _kimmiLaborFlauntFairly() {
    if (_chatCall.hasVideo()) return;
    if (callDuration - ltscd >= 40) {
      KimmiIndiaWaitressDock.instance.kimmiNoticeWaitressDoggyBasket(
        "${"kimmi_broderick_doggy_husband_tootsie".tr}(-3)",
      );
      kimmiDoWigDoggy(3);
    }
  }

  void kimmiNoticeDoggyLobbyUp() {
    KimmiFailedWebsite.kimmiNoticeDoggyLobbyUp(_chatCall.user.avatarUrl, () {
      kimmiDoWigDoggy(3);
    });
  }

  void _kimmiHusbandMusic() async {
    rtcManager.controlPreview(true);
    isReadyPreview.value = true;
    update([kimmi_fermion_speed]);
  }

  void _kimmiJenMusic() async {
    rtcManager.controlPreview(false);
  }

  void _kimmiMarineCheesecake() async {
    if (!joinChannelBool) return;
    joinChannelBool = false;
    rtcManager.leaveChannel();
  }

  void kimmiChanOverly() async {
    rtcManager.switchCamera();
  }

  void kimmiFantasyQuantityClue(Size size, Offset offset, Offset nextOffset) {
    double dx = 0;
    if (offset.dx + nextOffset.dx <= 0) {
      dx = 0;
    } else if (offset.dx + nextOffset.dx >= (size.width - 100)) {
      dx = size.width - 100;
    } else {
      dx = offset.dx + nextOffset.dx;
    }
    double dy = 0;
    if (offset.dy + nextOffset.dy >= (size.height)) {
      dy = size.height;
    } else if (offset.dy + nextOffset.dy <= 174) {
      dy = 174;
    } else {
      dy = offset.dy + nextOffset.dy;
    }
    rtcOffset.value = Offset(dx, dy);
    update([kimmi_putz_speed]);
  }

  void kimmiNoticeTraditionFinallyProtective(bool checkCoupon, int fromType) {
    if (KimmiSully.inRechargePage()) return;

    _popupsTimes++;

    KIMMI.toNamed(
      KimmiSully.KimmiSaturnScareNieceContainer,
      arguments: KimmiSaturnScareNieceHealer(fromType: fromType),
    );
  }

  void kimmiNoticeLatinoSharp(BuildContext context) {
    oldDy = rtcOffset.value.dy;
    if (oldDy >= MediaQuery.of(context).size.height - 400) {
      rtcOffset.value = Offset(
        rtcOffset.value.dx,
        MediaQuery.of(context).size.height - 400,
      );
    }
    update([kimmi_putz_speed]);

    final future = KimmiLatinoContainer.showGiftPanel(
      context,
      (gift) => kimmiUterusLatino(KimmiStormLatino.fromVoGift(gift)),
    );
    future.then((value) {
      rtcOffset.value = Offset(rtcOffset.value.dx, oldDy);
      update([kimmi_putz_speed]);
    });
  }

  int _kimmiPoopHoistScareFoundationWaitressDoggy() {
    return _chatCall.balance ??
        (lastTimeBalanceValue >= 0 ? lastTimeBalanceValue : 0);
  }

  void kimmiUterusLatino(KimmiStormLatino? gift, {int? begId}) async {
    if (gift == null) return;

    if (begId != null &&
        _kimmiPoopHoistScareFoundationWaitressDoggy() < (gift.price ?? 0)) {
      Get.defaultDialog(
        title: "kimmi_broderick_finally".tr,
        middleText: "kimmi_broderick_scare_torch_mrs_latino".tr.replaceFirst(
          "%s",
          _kimmiPoopHoistScareFoundationWaitressDoggy().toString(),
        ),
        confirm: ElevatedButton(
          onPressed: () {
            Get.back();
            kimmiNoticeTraditionFinallyProtective(
              false,
              PayFromType.FROM_IM_SEND_GIFT,
            );
          },
          child: Text("kimmi_broderick_finally".tr),
        ),
        cancel: ElevatedButton(
          onPressed: () {
            Get.back();
          },
          child: Text("kimmi_broderick_cabernet".tr),
        ),
      );
      return;
    }

    int callId = _chatCall.id;
    int? giftId = gift.id;
    int toUid = _chatCall.user.uid;
    String? animationUrl = gift.animationUrl;
    if (giftId == null) {
      return;
    }

    final req = SendChatCallGiftReq.create();

    req.callId = KimmiStarbucksJuda.toInt64(callId);

    req.giftId = KimmiStarbucksJuda.toInt64(giftId);

    req.toUid = KimmiStarbucksJuda.toInt64(toUid);

    req.quantity = 1;

    req.begId = KimmiStarbucksJuda.toInt64(begId ?? 0);
    GeneratedMessage? result = await KIMMI.socket.sendWithReturn(
      req,
      autoToastOnError: true,
    );

    if (result != null) {
      KimmiFeast me = KimmiFeast();
      me.uid = KIMMI.user().uid;
      me.nickName = KIMMI.user().nickName;
      me.avatarUrl = KIMMI.user().avatarUrl;

      KimmiWaitressDoggyLatino callGift = KimmiWaitressDoggyLatino();
      callGift.from = me;
      callGift.to = _chatCall.user;
      callGift.gift = gift;
      _kimmiMusicalWaitressDoggyLatino(callGift);

      if (animationUrl != null && animationUrl.isNotEmpty) {
        Get.toNamed(
          KimmiSully.KimmiSVGAContainer,
          arguments: KimmiWelcomeHealer(animationUrl, false),
        );
      }
    } else {}
    if (begId != null) {
      begGiftPsh = null;
      showAskGiftView.value = false;
      update([kimmi_kite_latino]);
    }
  }

  RtcEngine getEngine() {
    return rtcManager.getRtcEngine();
  }
}
