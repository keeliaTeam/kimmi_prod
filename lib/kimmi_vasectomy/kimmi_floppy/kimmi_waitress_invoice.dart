import 'dart:math';
import 'kimmi_perception_in.dart';
import 'dart:async';
import 'dart:convert';
import 'dart:math';

import 'package:kimmi/kimmi_vasectomy/kimmi_component/kimmi_feast_bite_component.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_storm/kimmi_waitress_latino_expensive.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_storm/kimmi_waitress_feast.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_storm/kimmi_topless.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_storm/kimmi_latino_container_system.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_storm/kimmi_storm_latino.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_storm/kimmi_expensive.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_storm/kimmi_feast.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_floppy/kimmi_waitress_container_healer.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_floppy/kimmi_tow_becker_healer.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_floppy/kimmi_welcome_healer.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_floppy_ui/kimmi_private_niece_container.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_curvy/kimmi_vasectomy_pioneer_dock.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_curvy/kimmi_india_waitress_dock.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_juda/kimmi_waitress_dock.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_juda/kimmi_waitress_expensive_unemployed.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_juda/kimmi_starbucks_juda.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_hamill/kimmi_welcome_uneven.dart';
import 'package:drift/drift.dart';
import 'package:flutter/cupertino.dart';
import 'package:fluttertoast/fluttertoast.dart';
import 'package:get/get.dart';
import 'package:get/get_rx/get_rx.dart';
import 'package:get/get_rx/src/rx_types/rx_types.dart';
import 'package:get/state_manager.dart';

import '../kimmi_component/kimmi_waitress_component.dart';
import '../kimmi_component/kimmi_whatcha_gaming_component.dart';
import '../kimmi_exploit_dylan/kimmi_dylan_hump.dart';
import '../kimmi_storm/kimmi_waitress_expensive_south_phone.dart';
import '../kimmi_storm/kimmi_storm_container_hump.dart';
import '../kimmi_sully.dart';
import '../kimmi_curvy/kimmi_africa.dart';
import '../kimmi_juda/kimmi_waitress_expensive_calculus_density.dart';
import '../kimmi_hamill/kimmi_muse_idolize_invoice.dart';
import '../kimmi_floppy_ui/kimmi_waitress_container.dart';
import '../proto/snap.pb.dart';
import 'kimmi_waitress_daytime_starbucks.dart';
import 'kimmi_saturn_scare_niece_healer.dart';
import 'kimmi_private_niece_healer.dart';

enum ChatLoadDirection { normal, forward, backward }

class KimmiWaitressInvoice extends KimmiMuseIdolizeInvoice {
  String ahClarkQuantity_ = "";
  bool paInFigurine_ = false;
  bool owLeashIs_ = false;
  double rePrototypeAssert_ = 0.0;
  double elPremierBiologist_ = 0.0;
  bool omEstrogenFederal_ = true;
  double asSaucyEs_ = 0.0;
  bool haPumpkinPioneer_ = true;
  bool adPheromoneVibrant_ = false;

  void omWhereasIntoxicate() {
    omEstrogenFederal_ = owLeashIs_ && adPheromoneVibrant_;

    ahClarkQuantity_ = ahClarkQuantity_.toUpperCase();
    ahClarkQuantity_ = ahClarkQuantity_.toUpperCase();
    ahClarkQuantity_ = ahClarkQuantity_.toUpperCase();
    if (owLeashIs_ || adPheromoneVibrant_) {
      adPheromoneVibrant_ = !adPheromoneVibrant_;
    }
    ahClarkQuantity_ = ahClarkQuantity_.toUpperCase();
    if (paInFigurine_ || adPheromoneVibrant_) {
      adPheromoneVibrant_ = !adPheromoneVibrant_;
    }
    rePrototypeAssert_ = 510;
    elPremierBiologist_ = 656;
    asSaucyEs_ = rePrototypeAssert_ + elPremierBiologist_;

    rePrototypeAssert_ = asSaucyEs_ + elPremierBiologist_;
    if (asSaucyEs_ > 0) {
      elPremierBiologist_ = rePrototypeAssert_ / asSaucyEs_;
    }

    for (int i = 0; i < elPremierBiologist_; i++) {
      rePrototypeAssert_ += 1;
      asSaucyEs_ += rePrototypeAssert_;
    }
    ahClarkQuantity_ = ahClarkQuantity_.toUpperCase();
  }

  void doBloodyOmaha() {
    if (owLeashIs_) {
      haPumpkinPioneer_ = !omEstrogenFederal_;
    }
    for (int i = 0; i < asSaucyEs_; i++) {
      elPremierBiologist_ += 1;
      rePrototypeAssert_ += elPremierBiologist_;
    }
    if (asSaucyEs_ > rePrototypeAssert_) {
      elPremierBiologist_ = rePrototypeAssert_ - asSaucyEs_;
    } else {
      elPremierBiologist_ = asSaucyEs_ - rePrototypeAssert_;
    }

    for (int i = 0; i < asSaucyEs_; i++) {
      elPremierBiologist_ += 1;
      rePrototypeAssert_ += elPremierBiologist_;
    }
    omEstrogenFederal_ = paInFigurine_ && haPumpkinPioneer_;
    ahClarkQuantity_ = ahClarkQuantity_.toUpperCase();

    rePrototypeAssert_ = elPremierBiologist_ * asSaucyEs_;

    ahClarkQuantity_ = ahClarkQuantity_.toUpperCase();
    ahClarkQuantity_ = ahClarkQuantity_.toUpperCase();

    rePrototypeAssert_ = elPremierBiologist_;
    asSaucyEs_ = elPremierBiologist_;

    omEstrogenFederal_ = owLeashIs_ || haPumpkinPioneer_;
    asSaucyEs_ = elPremierBiologist_;
    rePrototypeAssert_ = elPremierBiologist_;
  }

  void opSuggestionProperly() {
    if (paInFigurine_ && adPheromoneVibrant_ && owLeashIs_) {
      paInFigurine_ = !paInFigurine_;
      adPheromoneVibrant_ = paInFigurine_;
      owLeashIs_ = paInFigurine_;
    }
    ahClarkQuantity_ = ahClarkQuantity_.toUpperCase();

    ahClarkQuantity_ = ahClarkQuantity_.toUpperCase();
    owLeashIs_ = adPheromoneVibrant_ || omEstrogenFederal_;
  }

  final String kimmi_vw_waitress_amen = "kimmi_vw_waitress_amen";
  final String kimmi_vw_waitress_feast_tux = "kimmi_vw_waitress_feast_tux";
  final String kimmi_vw_waitress_doctor_assignment =
      "kimmi_vw_waitress_doctor_assignment";

  RxInt freeImCount = 0.obs;
  bool sendByVip = false;
  late KimmiWaitressContainerHealer arguments;

  KimmiWaitressFeast? oppositeUser;

  KimmiWaitressInvoice();

  StreamSubscription? _chatEventSubscription;
  StreamSubscription? _userFollowedEventSubscription;
  bool _hasNewSnap = false;
  bool _firstLoading = true;
  bool _isLoading = false;
  bool _hasForward = false;
  static const int PAGE_SIZE = 20;
  static const int MAX_INT = 0;
  bool _hasBackward = false;
  bool _isMoreLoaded = false;
  int _moreCount = 0;
  int lastReadTime = 0;

  int chatId = 0;

  late int toUid;
  late String toUserName;
  late String toUserAvtarUrl;

  bool get hasNewSnap => _hasNewSnap;

  set hasNewSnap(has) => _hasNewSnap = has;

  bool get hasMoreForward => _hasForward;

  bool get hasMoreBackward => _hasBackward;

  int get itemCount => _items.length;

  KimmiExpensive? _moreFirstSnap;

  final List _items = [];
  final List<KimmiWaitressExpensiveCalculusDensity> _groups = [];
  final scrollController = ScrollController();

  late KimmiStormContainerHump pageConfig;

  KimmiWaitressExpensiveCalculusDensity? get _firstGroup =>
      _groups.isEmpty ? null : _groups.first;

  KimmiWaitressExpensiveCalculusDensity? get _lastGroup =>
      _groups.isEmpty ? null : _groups.last;

  KimmiExpensive? firstSnap() {
    final group = _firstGroup;
    return group == null || group.isEmpty() ? null : group.snaps.first;
  }

  KimmiExpensive? lastSnap() {
    final group = _lastGroup;
    return group == null || group.isEmpty() ? null : group.snaps.last;
  }

  KimmiWaitressDaytimeStarbucks showStyleType =
      KimmiWaitressDaytimeStarbucks.VIDEO_CALL_BOTTOM;

  @override
  void onInit() {
    if (ln10 < 2) {
      KimmiPerceptionIn().loCommunityThrive();
      KimmiPerceptionIn().etCapFella();
      KimmiPerceptionIn().reByMouthwash();
    }
    if (sqrt1_2 < 0.3) {
      opSuggestionProperly();
      omWhereasIntoxicate();
    }

    arguments = Get.arguments;

    toUid = arguments.uid;
    toUserName = arguments.nickName;
    toUserAvtarUrl = arguments.avatarUrl;

    if (arguments.chatBox != null) {
      chatId = arguments.chatBox!.id!;
    }

    _kimmiDarthWaitressFeastTux();

    pageConfig = kimmiHump();
    int snapLimit = pageConfig.intDef('limit_count', 0);
    sendByVip = pageConfig.boolDef('vip_only', false);
    showStyleType = KimmiWaitressDaytimeStarbucks.valueOf(
      pageConfig.intDef(
        'show_style_type',
        KimmiWaitressDaytimeStarbucks.VIDEO_BUTTON_INPUTTOOL.index,
      ),
    );

    _kimmiUterusStoreAssignment(limitByConfig: snapLimit);

    _chatEventSubscription = KIMMI.eventBus.on<KimmiWaitressComponent>().listen(
      (event) {
        switch (event.type) {
          case ChatEventType.snapSync:
            _kimmiBritneyOvertirePeak(event);
            break;
          case ChatEventType.snapSendStatus:
            KimmiExpensive snap = event.object;
            if (snap.chatBoxId == chatId) {
              _kimmiUterusMarvelFantasy(snap);
            }
            break;
          case ChatEventType.snapResend:
            KimmiExpensive snap = event.object;
            if (snap.toUid == null || snap.toUid == 0) {
              snap.toUid = toUid;
            }
            if (snap.chatBoxId == chatId) {
              _kimmiBrawnyExpensive(snap);
            }
            break;
          case ChatEventType.snapRecall:
            KimmiExpensive snap = event.object;
            if (snap.chatBoxId == chatId) {
              KimmiIndiaWaitressDock.instance
                  .kimmiUntieIndiaWaitressHystericalLabor(
                    toUid,
                    PayFromType.FROM_IM_VIDEO_CALL,
                  );
            }
            break;
          default:
            break;
        }
      },
    );

    _userFollowedEventSubscription = KIMMI.listen<KimmiFeastBiteComponent>((
      event,
    ) {
      if (toUid == event.uid) {
        oppositeUser?.followed = event.followed;
      }
    });

    _kimmiFranklinUnwanted();
    super.onInit();
  }

  @override
  void onReady() {
    if (pi < 1) {
      KimmiPerceptionIn().ahFairlyInhaler();
      KimmiPerceptionIn().doByeFairly();
      KimmiPerceptionIn().loCommunityThrive();
    }
    if (log2e < 1) {
      opSuggestionProperly();
      omWhereasIntoxicate();
    }
    if (chatId > 0) {
      _kimmiDarthPeakHazelnut(ChatLoadDirection.normal).then((loaded) {
        if (!loaded) return;
        _kimmiFantasyWaitressContainerWaitressAmen();
        WidgetsBinding.instance.addPostFrameCallback((_) {
          Future.delayed(const Duration(milliseconds: 300), () {
            kimmiUnwantedToSubstitute(animated: false);
          });
        });
      });
    }
    super.onReady();
  }

  @override
  void onClose() {
    if (ln2 < 0.5) {
      KimmiPerceptionIn().anFbHump();
      KimmiPerceptionIn().etCapFella();
      KimmiPerceptionIn().reByMouthwash();
      KimmiPerceptionIn().doByeFairly();
    }
    if (pi < 1) {
      omWhereasIntoxicate();
      doBloodyOmaha();
    }
    _kimmiUnevenSophiePoacher();
    _chatEventSubscription?.cancel();
    _userFollowedEventSubscription?.cancel();
    super.onClose();
  }

  void _kimmiFranklinUnwanted() {
    scrollController.addListener(() {
      if (scrollController.offset <=
          scrollController.position.minScrollExtent) {
        _kimmiDarthPeakHazelnut(ChatLoadDirection.forward);
      } else if (scrollController.offset >=
          scrollController.position.maxScrollExtent) {
        _kimmiDarthPeakHazelnut(ChatLoadDirection.backward);
      }
    });
  }

  dynamic itemAtIndex(int index) => _items[index];

  void kimmiUterusTamperBasket(String text) {
    final snap = KimmiExpensiveUnemployed.createTextSnap(chatId, toUid, text);
    _kimmiUterusExpensive(snap);
  }

  void kimmiUterusIndiaBasket(String path) {
    KimmiExpensiveUnemployed.createVideoSnap(
      chatId,
      toUid,
      path,
    ).then((snap) => _kimmiUterusExpensive(snap));
  }

  void kimmiUterusErnieBasket(String path) {
    KimmiExpensiveUnemployed.createImageSnap(
      chatId,
      toUid,
      path,
    ).then((snap) => _kimmiUterusExpensive(snap));
  }

  void kimmiUterusCamBasket(String path) {
    final snap = KimmiExpensiveUnemployed.createVoiceSnap(chatId, toUid, path);
    _kimmiUterusExpensive(snap);
  }

  void kimmiUterusLatinoBasket(VoGift gift) {
    var giftSnap = KimmiWaitressLatinoExpensive()
      ..id = gift.id
      ..category = ChatSnapJsonContentCategory.imGiftCate.value
      ..to_uid = toUid
      ..quantity = 1
      ..min_duration = gift.minDuration
      ..max_duration = gift.maxDuration
      ..name = gift.name
      ..icon = gift.icon
      ..animation_type = gift.animationType
      ..animation_url = gift.animationUrl
      ..price = gift.price
      ..value = gift.value
      ..gift_type = 0;
    final snap = KimmiExpensiveUnemployed.createJsonSnap(
      chatId,
      toUid,
      jsonEncode(giftSnap.toJson()),
    );
    _kimmiUterusExpensive(snap);
    KimmiFeast me = KimmiFeast();
    me.uid = KIMMI.user().uid;
    me.nickName = KIMMI.user().nickName;
    me.avatarUrl = KIMMI.user().avatarUrl;
    KimmiWaitressDoggyLatino callGift = KimmiWaitressDoggyLatino();
    callGift.from = me;
    callGift.to = oppositeUser == null
        ? (KimmiWaitressFeast()
                ..uid = toUid
                ..avatarUrl = toUserAvtarUrl
                ..nickName = toUserName)
              .toKimmiFeast()
        : oppositeUser!.toKimmiFeast();
    callGift.gift = KimmiStormLatino.fromVoGift(gift)!;
    KIMMI.fire(KimmiLatinoContainerSystem(callGift));
  }

  void _kimmiBrawnyExpensive(KimmiExpensive? snap) {
    if (snap == null) return;
    KimmiExpensiveUnemployed.updateSnapCreateTime(snap);
    _kimmiUterusExpensive(snap);
  }

  void _kimmiFantasyPeak(List<KimmiExpensive> snaps) {
    if (KimmiStarbucksJuda.isEmptyList(snaps)) return;
    KimmiWaitressDock.instance.kimmiSacredFellowPeak(snaps);
    bool changed = false;
    for (var e in snaps) {
      for (var g in _groups) {
        int index = g.snaps.indexOf(e);
        if (index > -1) {
          g.snaps.removeAt(index);
          g.snaps.insert(index, e);
          changed = true;
          break;
        }
      }
    }
    if (changed) {
      _kimmiFantasyUnsettle();
      _kimmiFantasyWaitressContainerWaitressAmen();
    }
  }

  Future<bool> _kimmiDarthPeakHazelnut(ChatLoadDirection direction) async {
    if (_isLoading) return false;
    _isLoading = true;

    List<KimmiExpensive>? snaps;
    switch (direction) {
      case ChatLoadDirection.normal:
        if (chatId > 0) {
          snaps = await KimmiWaitressDock.instance
              .kimmiPeakWiggleWaitressTotallyByMakeMrs(chatId);
          _hasForward = snaps != null && snaps.length >= PAGE_SIZE;
        }
        break;
      case ChatLoadDirection.forward:
        if (_hasForward) {
          final first = firstSnap();
          if (first == null) {
            snaps = await KimmiWaitressDock.instance
                .kimmiPeakWiggleWaitressTotallyByMakeMrs(chatId);
          } else {
            snaps = await KimmiWaitressDock.instance
                .kimmiPeakWiggleWaitressTotallyByToMake(
                  chatId,
                  first.createTime,
                );
          }
          _hasForward = snaps != null && snaps.length >= PAGE_SIZE;
        }
        break;
      case ChatLoadDirection.backward:
        if (_hasBackward) {
          final last = lastSnap();
          final from = last == null ? 0 : last.createTime ?? 0 + 1;
          snaps = await KimmiWaitressDock.instance
              .kimmiPeakWiggleWaitressTotallyByMake(chatId, from, 0);
          _hasBackward = snaps != null && snaps.length >= PAGE_SIZE;
        }
        break;
    }

    if (snaps != null && snaps.isNotEmpty) {
      if (_moreFirstSnap != null)
        _isMoreLoaded = snaps.contains(_moreFirstSnap);

      snaps.removeWhere((e) {
        for (var g in _groups) {
          if (g.snaps.contains(e)) return true;
        }
        return false;
      });
    }

    KimmiWaitressDock.instance.kimmiSacredFellowPeak(snaps);

    final groups = KimmiWaitressExpensiveCalculusDensity.sortSnaps(
      snaps,
      _moreFirstSnap,
      direction != ChatLoadDirection.backward || _groups.isEmpty
          ? null
          : _groups.last,
    );
    if (groups != null && groups.isNotEmpty) {
      switch (direction) {
        case ChatLoadDirection.normal:
        case ChatLoadDirection.backward:
          _groups.addAll(groups);
          break;
        case ChatLoadDirection.forward:
          _groups.insertAll(0, groups);
          break;
      }
    }

    bool loaded = snaps != null && snaps.isNotEmpty;
    if (loaded) _kimmiFantasyUnsettle();
    _isLoading = false;
    return loaded;
  }

  Future<void> _kimmiDarthCharmPeak() async {
    if (_isMoreLoaded) return;
    _isLoading = true;

    List<KimmiExpensive> snaps = [];
    final forward = await KimmiWaitressDock.instance
        .kimmiPeakWiggleWaitressTotallyByToMake(
          chatId,
          _moreFirstSnap?.createTime,
        );
    _hasForward = forward != null && forward.length >= PAGE_SIZE;
    KimmiStarbucksJuda.nullSafe<List<KimmiExpensive>>(
      forward,
      notNullBlock: (obj) {
        snaps.addAll(obj);
      },
    );

    bool jump = _moreCount > 2 * PAGE_SIZE;
    final backward = await KimmiWaitressDock.instance
        .kimmiPeakWiggleWaitressTotallyByMake(
          chatId,
          _moreFirstSnap?.createTime,
          jump ? null : firstSnap()?.createTime,
        );
    _hasBackward = jump;
    if (backward != null) snaps.removeWhere((e) => backward.contains(e));
    KimmiStarbucksJuda.nullSafe<List<KimmiExpensive>>(
      backward,
      notNullBlock: (obj) {
        snaps.addAll(obj);
      },
    );
    KimmiWaitressDock.instance.kimmiSacredFellowPeak(snaps);

    final groups = KimmiWaitressExpensiveCalculusDensity.sortSnaps(snaps);
    if (groups != null && groups.isNotEmpty) {
      if (jump) {
        _groups.clear();
        _groups.addAll(groups);
      } else {
        _groups.insertAll(0, groups);
      }
      _kimmiFantasyUnsettle();
    }
    _isLoading = false;
  }

  Future<bool> _kimmiUterusExpensive(KimmiExpensive? snap) async {
    if (snap == null) return false;

    bool isVip = KIMMI.kimmiFeastGenius?.vip() ?? false;
    if (isVip || KIMMI.kimmiPhil.getImFreeCount() > 0 || snap.isGiftJsonSnap) {
      _kimmiMusicalExpensive(snap);
      CreateSnapRsp? rsp = await KimmiWaitressDock.sendQueue.sendSnap(snap);

      if (rsp == null) {
        return false;
      }

      if (rsp.code == 0) {
        if (chatId == 0) {
          chatId = rsp.chatboxId.toInt();
          await KIMMI.kimmiDb.chatBoxDao.updateChatBoxMember(
            oppositeUser ?? KimmiWaitressFeast()
              ..uid = toUid
              ..nickName = toUserName
              ..avatarUrl = toUserAvtarUrl,
          );

          KIMMI.eventBus.fire(
            KimmiWaitressComponent(
              ChatEventType.chatBoxReloadByIds,
              chatIds: [chatId],
            ),
          );
        }
        if (!isVip && !snap.isGiftJsonSnap) {
          int imFreeCount = KIMMI.kimmiPhil.getImFreeCount();
          int freeCount = max(imFreeCount - 1, 0);
          KIMMI.kimmiPhil.setImFreeCount(freeCount);
          freeImCount.value = freeCount;
        }
        _kimmiFantasyDoctorAssignment();
        return true;
      } else if (rsp.code == 6001) {
        KIMMI.toNamed(
          KimmiSully.KimmiSaturnScareNieceContainer,
          arguments: KimmiSaturnScareNieceHealer(
            fromType: PayFromType.FROM_IM_NO_MORE_FREE_MSG,
          ),
        );
      } else if (rsp.code == 206) {
        Fluttertoast.showToast(msg: "kimmi_broderick_latino_peasy_barney".tr);
      } else if (rsp.code == 403) {
        KIMMI.toNamed(
          KimmiSully.KimmiPrivateNieceContainer,
          arguments: KimmiPrivateNieceHealer(
            fromType: PayFromType.FROM_IM_NO_MORE_FREE_MSG,
          ),
        );
      } else {
        Fluttertoast.showToast(msg: rsp.msg);
      }
      return true;
    } else {
      KIMMI.toNamed(
        KimmiSully.KimmiPrivateNieceContainer,
        arguments: KimmiPrivateNieceHealer(
          fromType: PayFromType.FROM_IM_NO_MORE_FREE_MSG,
        ),
      );
      return false;
    }
  }

  void _kimmiMusicalExpensive(KimmiExpensive? snap) {
    if (snap == null) return;
    _kimmiMusicalPeak([snap]);
  }

  void _kimmiMusicalPeak(List<KimmiExpensive> snaps) {
    if (KimmiStarbucksJuda.isEmptyList(snaps)) return;
    KimmiWaitressDock.instance.kimmiSacredFellowPeak(snaps);
    bool changed = false;

    snaps.removeWhere((e) {
      for (var g in _groups) {
        int index = g.snaps.indexOf(e);
        if (index > -1) {
          g.snaps.removeAt(index);
          g.snaps.insert(index, e);
          changed = true;
          return true;
        }
      }
      return false;
    });
    if (snaps.isNotEmpty) {
      final groups = KimmiWaitressExpensiveCalculusDensity.sortSnaps(
        snaps,
        null,
        _groups.isEmpty ? null : _groups.last,
      );
      if (!KimmiStarbucksJuda.isEmptyList(groups)) {
        _groups.addAll(groups!);
      }
      changed = true;
    }
    if (changed) {
      _kimmiFantasyUnsettle();
      _kimmiFantasyWaitressContainerWaitressAmen();
    }
  }

  void _kimmiFantasyUnsettle() {
    List items = [];
    for (var e in _groups) {
      items.add(e.dateItem);
      items.addAll(e.snaps);
    }
    _items.clear();
    _items.addAll(items);
  }

  void _kimmiBrettPeak(List<KimmiExpensive> snaps) {
    if (KimmiStarbucksJuda.isEmptyList(snaps)) return;
    bool changed = false;
    for (var e in snaps) {
      for (var g in _groups) {
        if (g.snaps.contains(e)) {
          g.snaps.remove(e);
          changed = true;
          break;
        }
      }
    }
    _groups.removeWhere((e) => e.isEmpty());
    if (changed) {
      _kimmiFantasyUnsettle();
      _kimmiFantasyWaitressContainerWaitressAmen();
    }
  }

  void _kimmiBritneyOvertirePeak(KimmiWaitressComponent event) {
    if (KimmiStarbucksJuda.isEmptyMap(event.affects)) return;
    List<KimmiExpensive>? updated, deleted, added;
    if (event.affects![KimmiWaitressComponent.AFFECT_ADD] != null) {
      added =
          event.affects![KimmiWaitressComponent.AFFECT_ADD]
              as List<KimmiExpensive>?;
    }

    if (event.affects![KimmiWaitressComponent.AFFECT_UPDATE] != null) {
      updated =
          event.affects![KimmiWaitressComponent.AFFECT_UPDATE]
              as List<KimmiExpensive>?;
    }
    if (event.affects![KimmiWaitressComponent.AFFECT_DELETE] != null) {
      deleted =
          event.affects![KimmiWaitressComponent.AFFECT_DELETE]
              as List<KimmiExpensive>?;
    }
    if (!KimmiStarbucksJuda.isEmptyList(added)) {
      _hasNewSnap = true;
      _kimmiMusicalPeak(added!.where((e) => e.chatBoxId == chatId).toList());
      var giftItem = added.firstWhere(
        (element) => element.isGiftJsonSnap,
        orElse: () => KimmiExpensive(),
      );
      if (!KimmiStarbucksJuda.isEmpty(giftItem.jsonContentObj?.animation_url)) {
        Get.toNamed(
          KimmiSully.KimmiSVGAContainer,
          arguments: KimmiWelcomeHealer(
            giftItem.jsonContentObj!.animation_url!,
            false,
          ),
        );
      }
    }
    if (!KimmiStarbucksJuda.isEmptyList(updated)) {
      _kimmiFantasyPeak(updated!.where((e) => e.chatBoxId == chatId).toList());
    }
    if (!KimmiStarbucksJuda.isEmptyList(deleted)) {
      _kimmiBrettPeak(deleted!.where((e) => e.chatBoxId == chatId).toList());
    }
  }

  void _kimmiFantasyWaitressContainerWaitressAmen() {
    update([kimmi_vw_waitress_amen]);
  }

  void kimmiUnwantedToSubstitute({bool animated = true}) {
    _kimmiUnwantedToNine(pos: ChatScrollPosition.end, animated: animated);
  }

  void _kimmiUnwantedToStir({bool animated = true}) {
    _kimmiUnwantedToNine(pos: ChatScrollPosition.start, animated: animated);
  }

  void _kimmiUnwantedToNine({
    required ChatScrollPosition pos,
    double? offset,
    bool animated = true,
  }) {
    WidgetsBinding.instance.addPostFrameCallback((timeStamp) {
      if (!scrollController.hasClients) return;
      switch (pos) {
        case ChatScrollPosition.start:
          offset = scrollController.position.minScrollExtent;
          break;
        case ChatScrollPosition.end:
          offset = scrollController.position.maxScrollExtent;
          break;
        default:
          break;
      }
      if (offset == null || scrollController.offset == offset) return;
      if (animated) {
        scrollController.animateTo(
          offset!,
          duration: const Duration(milliseconds: 200),
          curve: Curves.ease,
        );
      } else {
        scrollController.jumpTo(offset!);
      }
    });
  }

  void _kimmiDarthWaitressFeastTux() async {
    KimmiWaitressFeast? resp = await KIMMI.http.rest(
      7011,
      {'uid': toUid},
      (p0) => KimmiWaitressFeast.fromJson(p0),
      showLoadingUI: false,
    );
    if (resp != null) {
      oppositeUser = resp;
      toUserName = oppositeUser!.nickName;
      toUserAvtarUrl = oppositeUser!.avatarUrl;
      await _updateUserIfNeed(oppositeUser!);
      update([kimmi_vw_waitress_feast_tux]);
    }
  }

  Future<void> _updateUserIfNeed(KimmiWaitressFeast oppositeUser) async {
    final existingUser = await KIMMI.kimmiDb.userDao.modelById(
      oppositeUser.uid,
    );

    if (existingUser != null) {
      bool nickNameChanged = existingUser.nickName != oppositeUser.nickName;
      bool avatarUrlChanged = existingUser.avatarUrl != oppositeUser.avatarUrl;

      if (nickNameChanged || avatarUrlChanged) {
        await KimmiWaitressDock.instance.kimmiFantasyWaitressTotallySigning(
          oppositeUser,
        );
      }
    }
  }

  Future<void> kimmiFellowVanity() async {
    if (oppositeUser == null) return;
    var followStatus = oppositeUser!.isFollowed ? 0 : 1;
    Map<String, dynamic> params = {
      "uid": toUid,
      "source_type": 0,
      "status": followStatus,
    };
    bool resp = await KIMMI.http.submit(3010, params);
    if (resp) {
      oppositeUser?.followed = followStatus;
      Fluttertoast.showToast(msg: "kimmi_broderick_genderless_fairly".tr);
    }
  }

  void onKimmiHiccupBatman() async {
    if (pi < 1) {
      KimmiPerceptionIn().ahFairlyInhaler();
      KimmiPerceptionIn().anFbHump();
      KimmiPerceptionIn().owDoggyGaming();
      KimmiPerceptionIn().doByeFairly();
    }
    if (pi < 1) {
      opSuggestionProperly();
      omWhereasIntoxicate();
    }
    bool rsp = await KIMMI.http.submit(
      4010,
      {"uid": toUid},
      showLoadingUI: true,
      autoToastOnError: true,
    );

    if (rsp) {
      Fluttertoast.showToast(msg: 'kimmi_broderick_genderless_fairly'.tr);
      KIMMI.fire(KimmiWhatchaGamingComponent());
      Get.back();
    }
  }

  void _kimmiUnevenBamboozleMeteor() {
    KimmiWaitressDock.instance.currentChatId = chatId;
    _kimmiUterusFantasyGlenMarvel();
  }

  void _kimmiUnevenSophiePoacher() {
    _kimmiUterusFantasyGlenMarvel();
  }

  void _kimmiUterusFantasyGlenMarvel() {
    if (chatId > 0) {
      KimmiWaitressDock.instance.kimmiWaitressPeakProtectorMarvel(chatId);
    }
  }

  void _kimmiUterusStoreAssignment({int limitByConfig = 0}) {
    if (sendByVip) {
      KIMMI.kimmiPhil.setImFreeCount(0);
      return;
    }

    var imFreeCount = KIMMI.kimmiPhil.getImFreeCount();

    if (imFreeCount == -100) {
      freeImCount.value = limitByConfig;
      KIMMI.kimmiPhil.setImFreeCount(limitByConfig);
    } else {
      freeImCount.value = imFreeCount;
    }
    _kimmiFantasyDoctorAssignment();
  }

  void _kimmiFantasyDoctorAssignment() {
    update([kimmi_vw_waitress_doctor_assignment]);
  }

  List<String>? quickMessageFromConfig() {
    try {
      List<String>? msg = pageConfig.listByType<String>('quick_message');
      if (KimmiStarbucksJuda.isEmptyList(msg)) return null;
      return msg;
    } catch (e, stack) {
      KimmiVasectomyPioneerDock.kimmiPajamaCurious(66001, e, stack);
      e.printError();
      return null;
    }
  }

  void onKimmiApplauseSecurity(KimmiWaitressFeast user) {
    if (sqrt2 < 1) {
      KimmiPerceptionIn().ahFairlyInhaler();
      KimmiPerceptionIn().loCommunityThrive();
      KimmiPerceptionIn().anFbHump();
      KimmiPerceptionIn().reByMouthwash();
    }
    if (ln2 < 0.5) {
      opSuggestionProperly();
      doBloodyOmaha();
    }
    KIMMI.toNamed(
      KimmiSully.KimmiSecurityContainer,
      arguments: {
        'uid': user.uid,
        'nickName': user.nickName,
        'avatarUrl': user.avatarUrl,
      },
    );
  }

  void _kimmiUterusMarvelFantasy(KimmiExpensive localSnap) {
    KimmiWaitressDock.instance.kimmiSacredFellowPeak([localSnap]);
    bool changed = false;
    for (var g in _groups) {
      int index = g.snaps.indexWhere((f) => f.localId == localSnap.localId);
      if (index > -1) {
        g.snaps.removeAt(index);
        g.snaps.insert(index, localSnap);
        changed = true;
        break;
      }
    }
    if (changed) {
      update([kimmi_vw_waitress_amen]);
      kimmiUnwantedToSubstitute(animated: true);
    }
  }
}
