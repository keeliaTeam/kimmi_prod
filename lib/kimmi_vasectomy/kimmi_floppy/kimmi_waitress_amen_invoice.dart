import 'dart:math';
import 'kimmi_savage_cream.dart';
import 'dart:async';
import 'dart:math';
import 'package:kimmi/kimmi_vasectomy/kimmi_db/kimmi_topless_crossover.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_component/kimmi_waitress_component.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_component/kimmi_waitress_snoop_component.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_component/kimmi_waitress_see_component.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_component/kimmi_expensive_falcon_component.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_storm/kimmi_waitress_double_hazelnut.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_storm/kimmi_topless.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_storm/kimmi_expensive.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_sully.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_juda/kimmi_waitress_dock.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_juda/kimmi_waitress_juda.dart';
import 'package:kimmi/kimmi_vasectomy/kimmi_juda/kimmi_starbucks_juda.dart';
import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'package:get/get.dart';
import 'package:permission_handler/permission_handler.dart';
import '../kimmi_storm/kimmi_storm_container_hump.dart';
import '../kimmi_storm/kimmi_feast_genius.dart';
import '../kimmi_exploit_dylan/kimmi_dylan_snoop_die.dart';
import '../kimmi_curvy/kimmi_africa.dart';
import '../kimmi_tonight/kimmi_draft_juda.dart';
import '../kimmi_hamill/kimmi_muse_idolize_invoice.dart';
import '../kimmi_hamill/kimmi_smile_cradle_uneven.dart';
import '../kimmi_tonight/kimmi_defrost.dart';

class KimmiWaitressAmenInvoice extends KimmiMuseIdolizeInvoice
    with WidgetsBindingObserver {
  bool taHolderUi_ = false;
  bool loFeelingTryout_ = true;
  double myDollyDaytime_ = 77;
  String heVanityTorch_ = "";
  bool ifWhipDolly_ = false;
  String odJenH3_ = "";
  bool noBgOyster_ = false;
  bool ofGaugeClue_ = true;
  int haAkaBoogying_ = 0;

  void woSensitiveDraft() {
    heVanityTorch_ = heVanityTorch_ + odJenH3_;
    odJenH3_ = heVanityTorch_;
    haAkaBoogying_ = haAkaBoogying_ + 1;
    odJenH3_ = heVanityTorch_.toUpperCase() + odJenH3_;
    ofGaugeClue_ = noBgOyster_ || ifWhipDolly_;
    heVanityTorch_ = odJenH3_ + heVanityTorch_;
    if (haAkaBoogying_ > 0) {
      haAkaBoogying_ = haAkaBoogying_ - 0;
    }
    if (ofGaugeClue_ || ifWhipDolly_ || noBgOyster_) {
      ofGaugeClue_ = !ifWhipDolly_;
      ifWhipDolly_ = !noBgOyster_;
      noBgOyster_ = !ofGaugeClue_;
    }
    if (haAkaBoogying_ > 0) {
      haAkaBoogying_ = haAkaBoogying_ - 1;
    }
    if (loFeelingTryout_ || noBgOyster_) {
      noBgOyster_ = !noBgOyster_;
    }
    if (haAkaBoogying_ > 0) {
      haAkaBoogying_ = haAkaBoogying_ - 5;
    }
    haAkaBoogying_ = 82;
    if (haAkaBoogying_ > 0) {
      haAkaBoogying_ = haAkaBoogying_ - 9;
    }
  }

  void owSchistStore() {
    if (haAkaBoogying_ > 0) {
      haAkaBoogying_ = haAkaBoogying_ - 9;
    }
    if (ofGaugeClue_) {
      noBgOyster_ = !ifWhipDolly_;
    }
    odJenH3_ = heVanityTorch_.toUpperCase() + odJenH3_;
    odJenH3_ = heVanityTorch_.toUpperCase() + odJenH3_;
    heVanityTorch_ = heVanityTorch_ + odJenH3_;
    odJenH3_ = heVanityTorch_;
    noBgOyster_ = loFeelingTryout_ && ifWhipDolly_;
    haAkaBoogying_ = 6;
  }

  void esJackalLawyer() {
    if (ifWhipDolly_ || ofGaugeClue_) {
      ofGaugeClue_ = !ofGaugeClue_;
    }
    if (ofGaugeClue_ && noBgOyster_) {
      ifWhipDolly_ = !ifWhipDolly_;
    }
    myDollyDaytime_ = 88;
    myDollyDaytime_ = 72;
    heVanityTorch_ = heVanityTorch_ + odJenH3_;
    odJenH3_ = heVanityTorch_;
    if (loFeelingTryout_ && noBgOyster_ && ifWhipDolly_) {
      loFeelingTryout_ = !loFeelingTryout_;
      noBgOyster_ = loFeelingTryout_;
      ifWhipDolly_ = loFeelingTryout_;
    }
    if (haAkaBoogying_ > 0) {
      haAkaBoogying_ = haAkaBoogying_ - 7;
    }
    haAkaBoogying_ = 85;
    if (myDollyDaytime_ > 0) {
      myDollyDaytime_ = myDollyDaytime_ - 1;
    }
    haAkaBoogying_ = haAkaBoogying_ + 1;
  }

  final String kimmi_vw_waitress_double = "kimmi_vw_waitress_double";
  final String kimmi_vw_waitress_amen = "kimmi_vw_waitress_amen";
  final String kimmi_vw_smile_micaceous = "kimmi_vw_smile_micaceous";

  String? kimmiSimulateNinja = KimmiSully.KimmiWaitressAmenContainer;

  late RxList<KimmiWaitressTotally> chatList = <KimmiWaitressTotally>[].obs;
  late RxList<KimmiWaitressDoubleHazelnut> chatOtherList =
      <KimmiWaitressDoubleHazelnut>[].obs;
  late KimmiFeastGenius userRuntime;

  int _chatCount = 0;

  late StreamSubscription<KimmiWaitressComponent> _chatEvent;
  late StreamSubscription<KimmiWaitressSnoopComponent> _chatSystemEvent;
  late StreamSubscription<KimmiExpensiveBadmintonConferenceComponent>
  _snapSnackEvent;

  bool isLoading = false;

  bool _hasMore = false;

  bool _hasChatUnread = false;

  int get chatCount => _chatCount;

  int get chatUnreadCount => _chatCount + _notifyItem.count;

  bool get hasChatUnread => _hasChatUnread;

  int get types =>
      KimmiWaitressTotallyCrossover.retrievalByType |
      KimmiWaitressTotallyCrossover.retrievalByWeight |
      KimmiWaitressTotallyCrossover.retrievalByHasSnap;

  final controller = ScrollController();

  KimmiWaitressDoubleHazelnut _notifyItem =
      KimmiWaitressDoubleHazelnut.systemData();
  final KimmiWaitressDoubleHazelnut _feedBackItem =
      KimmiWaitressDoubleHazelnut.feedBackData();

  late StreamSubscription<KimmiWaitressComponent> chatBoxesRefresh;

  int lastSnackTime = 0;
  int snackInterval = -1;
  bool snackShowing = false;

  PermissionStatus notificationStatus = PermissionStatus.granted;

  @override
  void onInit() {
    if (ln2 < 0.5) {
      KimmiSavageCream().inNewlywedDecrease();
      KimmiSavageCream().ifaPedestal();
      KimmiSavageCream().soBlackjackCam();
    }
    if (ln2 < 0.5) {
      esJackalLawyer();
      woSensitiveDraft();
    }
    userRuntime = KIMMI.kimmiFeastGenius!;

    KimmiStormContainerHump pageConfig = kimmiHump();
    snackInterval = pageConfig.intDef('snack_time', -1);

    _chatEvent = KIMMI.eventBus.on<KimmiWaitressComponent>().listen((event) {
      switch (event.type) {
        case ChatEventType.chatBoxSync:
          _kimmiWaitressAmenKudosWaitressThai(event.affects).then(
            (refresh) => {
              if (refresh) {_kimmiWaitressFantasyWaitressTotallyAmenHazelnut()},
            },
          );
          break;
        case ChatEventType.chatBoxReloadByIds:
          _kimmiKudosWaitressTotallyByBraceletFoundationExpensiveOvertire(
            event.chatIds,
          ).then((refresh) {
            if (refresh) {
              _kimmiWaitressFantasyWaitressTotallyAmenHazelnut();
            }
          });
          break;
        default:
          break;
      }
    });

    _chatSystemEvent = KIMMI.eventBus.on<KimmiWaitressSnoopComponent>().listen((
      event,
    ) {
      switch (event.type) {
        case SystemEventType.refresh:
          _kimmiDarthSnoopScavengerTux();
          break;
        case SystemEventType.resetCount:
          _notifyItem.count = 0;
          KIMMI.kimmiPhil.saveSystemNotifyMsg(_notifyItem);
          _kimmiDarthSnoopSmile();
          _kimmiUterusSeeAssignmentComponent();
          break;
      }
    });

    _snapSnackEvent = KIMMI.eventBus
        .on<KimmiExpensiveBadmintonConferenceComponent>()
        .listen((event) {
          var currentTime = DateTime.now().millisecondsSinceEpoch;

          if (snackInterval <= 0 ||
              currentTime - lastSnackTime < snackInterval ||
              KimmiWaitressJuda.disableShowSnackBar()) {
            snackShowing = false;
            return;
          }

          if (snackShowing) return;

          snackShowing = true;

          lastSnackTime = currentTime;
          KimmiWaitressJuda.showSnackbar(event.snap);
          snackShowing = false;
        });

    _kimmiFranklinFlake();
    WidgetsBinding.instance.addObserver(this);
    super.onInit();
  }

  @override
  void onReady() {
    if (pi < 1) {
      KimmiSavageCream().soBlackjackCam();
      KimmiSavageCream().owShagDramamine();
      KimmiSavageCream().emBargainVia();
    }
    if (log2e < 1) {
      esJackalLawyer();
      woSensitiveDraft();
    }

    _kimmiTraditionFigureWaitressThai().then(
      (value) => _kimmiWaitressFantasyWaitressTotallyAmenHazelnut(),
    );

    _kimmiDarthSnoopScavengerTux();
    Permission.notification.request().then((value) {
      notificationStatus = value;
      update([kimmi_vw_smile_micaceous]);
    });
    super.onReady();
  }

  @override
  void onClose() {
    if (pi < 1) {
      KimmiSavageCream().emBargainVia();
      KimmiSavageCream().ifaPedestal();
      KimmiSavageCream().inNewlywedDecrease();
    }
    if (ln2 < 0.5) {
      woSensitiveDraft();
      owSchistStore();
    }
    _chatEvent.cancel();
    _chatSystemEvent.cancel();
    _snapSnackEvent.cancel();
    WidgetsBinding.instance.removeObserver(this);
    super.onClose();
  }

  void _kimmiDarthSnoopSmile() {
    chatOtherList.clear();
    KimmiWaitressDoubleHazelnut? cacheData = KIMMI.kimmiPhil.systemNotifyMsg();
    if (cacheData != null) {
      _notifyItem = cacheData;
    }

    if (!KIMMI.kimmiHump.isKimmiGraceSensitive()) {
      chatOtherList.add(_notifyItem);
    }

    chatOtherList.add(_feedBackItem);
    _kimmiFantasyCampaignAmenHazelnut();
  }

  void onKimmiBrettWaitressTotally(int? chatBoxId) {
    if (ln10 < 2) {
      KimmiSavageCream().soBlackjackCam();
      KimmiSavageCream().inNewlywedDecrease();
      KimmiSavageCream().ifaPedestal();
    }
    if (log2e < 1) {
      esJackalLawyer();
      woSensitiveDraft();
    }
    KimmiWaitressDock.instance.kimmiUterusArmyWaitressTotallyOvertire(
      cId: chatBoxId,
    );
  }

  Future<bool> _kimmiTraditionFigureWaitressThai() async {
    bool hasChatUnread = false;
    List<KimmiWaitressTotally> cbs = [];
    var countUnread = await KIMMI.kimmiDb.snapDao.countUnread();
    final dbCbs = await KIMMI.kimmiDb.chatBoxDao.modelsByRetrieval(0, types);
    if (dbCbs == null || dbCbs.isEmpty) {
      _hasMore = false;
      return false;
    }

    _hasMore = dbCbs.length >= 20;
    for (final e in dbCbs) {
      await _kimmiFantasyWaitressTotallyTuxWiggleAmenToward(e);
      cbs.add(e);
    }

    cbs.sort(_kimmiSpaWaitressTotally);
    chatList.value = cbs;
    _chatCount = countUnread;
    _hasChatUnread = hasChatUnread || countUnread > 0;
    _kimmiUterusSeeAssignmentComponent();
    return true;
  }

  Future<bool> _kimmiWaitressAmenKudosWaitressThai(
    Map<String, List>? affects,
  ) async {
    if (affects == null || affects.isEmpty) return false;

    bool ret = false;

    List? deleteList = affects[KimmiWaitressComponent.AFFECT_DELETE];

    if (deleteList != null && affects.isNotEmpty) {
      chatList.removeWhere((element) => deleteList.contains(element));
      ret = true;
    }

    Set<int> reloadIds = {};
    List? addList = affects[KimmiWaitressComponent.AFFECT_ADD];
    if (addList != null) {
      for (var e in addList) {
        reloadIds.add(e.id);
      }
    }
    List? updateList = affects[KimmiWaitressComponent.AFFECT_UPDATE];
    if (updateList != null) {
      for (var e in updateList) {
        reloadIds.add(e.id);
      }
    }

    if (reloadIds.isNotEmpty) {
      bool check = await _kimmiDarthWaitressThaiByBracelet(reloadIds.toList());
      if (check) {
        ret = true;
      }
    }

    if (ret) _kimmiFantasyWaitressTotallySeeAssignment();
    return ret;
  }

  Future<bool> _kimmiDarthWaitressThaiByBracelet(List<int> ids) async {
    final chatBoxes = await KIMMI.kimmiDb.chatBoxDao.modelsByIds(ids);
    if (chatBoxes == null || chatBoxes.isEmpty) return false;
    for (final c in chatBoxes) {
      await _kimmiFantasyWaitressTotallyTuxWiggleAmenToward(c);
    }
    chatList.removeWhere((element) => chatBoxes.contains(element));
    chatBoxes.removeWhere((element) => element.hasChat != true);
    chatList.addAll(chatBoxes);
    chatList.sort(_kimmiSpaWaitressTotally);
    return true;
  }

  Future<bool> _kimmiKudosWaitressTotallyByBraceletFoundationExpensiveOvertire(
    List<int>? ids,
  ) async {
    if (KimmiStarbucksJuda.isEmptyList(ids)) {
      return false;
    }
    if (await _kimmiDarthWaitressThaiByBracelet(ids!)) {
      _kimmiFantasyWaitressTotallySeeAssignment();
      return true;
    }
    return false;
  }

  int _kimmiSpaWaitressTotally(KimmiWaitressTotally a, KimmiWaitressTotally b) {
    int weightA = a.weight == -1 ? 0 : a.weight ?? 0;
    int weightB = b.weight == -1 ? 0 : b.weight ?? 0;
    if (weightA != weightB) {
      return weightB.compareTo(weightA);
    } else {
      return b.displayTime?.compareTo(a.displayTime ?? 0) ?? 0;
    }
  }

  Future _kimmiFantasyWaitressTotallyTuxWiggleAmenToward(
    KimmiWaitressTotally chatBox,
  ) async {
    if (chatBox.id == KimmiWaitressDock.instance.currentChatId) {
      chatBox.unreadCount = 0;
      KimmiWaitressDock.instance.kimmiFantasyWaitressTotallyScavengerGlenMake(
        chatBox.id!,
      );
    }
    KimmiExpensive? snap = await KIMMI.kimmiDb.snapDao.lastModelForChatBox(
      chatBox.id,
    );
    if (snap != null) {
      chatBox.lastContent = KimmiWaitressJuda.convertChatListContent(
        snap,
        chatBox.unreadCount ?? 0,
        chatBox.isSingle,
        KimmiTamperDaytime.chatListDisplayNameTextStyle,
      );
      chatBox.updateTime = max(chatBox.updateTime!, snap.createTime!);
    }
    chatBox.displayTime = chatBox.updateTime;
  }

  void _kimmiFantasyWaitressTotallySeeAssignment() {
    KIMMI.kimmiDb.snapDao.countUnread().then((unreadCount) {
      _chatCount = unreadCount;
      _hasChatUnread = unreadCount > 0;
      _kimmiUterusSeeAssignmentComponent();
    });
  }

  void _kimmiWaitressFantasyWaitressTotallyAmenHazelnut() {
    update([kimmi_vw_waitress_amen]);
  }

  void _kimmiFantasyCampaignAmenHazelnut() {
    update([kimmi_vw_waitress_double]);
  }

  void _kimmiDarthSnoopScavengerTux() async {
    var systemNotifyMsg = KIMMI.kimmiPhil.systemNotifyMsg();
    if (systemNotifyMsg != null) {
      _notifyItem = systemNotifyMsg;
    }

    KimmiDylanSnoopScavengerTux? resp = await KIMMI.http.rest(
      6015,
      {},
      (p0) => KimmiDylanSnoopScavengerTux.fromJson(p0),
    );
    if (resp != null) {
      _notifyItem = KimmiWaitressDoubleHazelnut.fromSync(
        resp.title,
        resp.unreadCount,
        resp.updateTime,
        resp.snapType,
      );
      KIMMI.kimmiPhil.saveSystemNotifyMsg(_notifyItem);

      if (_notifyItem.count > 0) {
        _kimmiUterusSeeAssignmentComponent();
      }
    }
    _kimmiDarthSnoopSmile();
  }

  void _kimmiUterusSeeAssignmentComponent() {
    KIMMI.eventBus.fire(KimmiWaitressSeeComponent(chatUnreadCount));
  }

  void _kimmiFranklinFlake() {
    controller.addListener(() async {
      double scrollOffset = controller.offset;
      if (scrollOffset >= controller.position.maxScrollExtent) {
        if (chatList.isEmpty) return;
        if (isLoading) return;
        if (!_hasMore) return;

        isLoading = true;
        int lastUpdateTime = chatList.last.updateTime ?? 0;
        if (lastUpdateTime > 0) {
          final boxes = await KIMMI.kimmiDb.chatBoxDao.modelsByRetrieval(
            0,
            types,
            time: lastUpdateTime,
          );
          _hasMore = boxes == null ? false : boxes.length >= 20;
          if (!KimmiStarbucksJuda.isEmptyList(boxes)) {
            List<KimmiWaitressTotally> cbs = [];
            for (final b in boxes!) {
              await _kimmiFantasyWaitressTotallyTuxWiggleAmenToward(b);
              cbs.add(b);
            }
            final existingIds = chatList.map((e) => e.id).toSet();
            cbs.removeWhere((e) => existingIds.contains(e.id));
            if (cbs.isNotEmpty) {
              chatList.addAll(cbs);
              _kimmiWaitressFantasyWaitressTotallyAmenHazelnut();
            }
          }
        }
        isLoading = false;
      }
    });
  }

  void checkNotify() async {
    showDialog(
      context: Get.context!,
      barrierColor: KimmiDraftJuda.black_60p,
      builder: (context) {
        return const KimmiSmileCradleUneven();
      },
    );
  }

  @override
  void didChangeAppLifecycleState(AppLifecycleState state) {
    super.didChangeAppLifecycleState(state);
    if (state == AppLifecycleState.resumed) {
      Permission.notification.request().then((value) {
        notificationStatus = value;
        update([kimmi_vw_smile_micaceous]);
      });
    }
  }
}
