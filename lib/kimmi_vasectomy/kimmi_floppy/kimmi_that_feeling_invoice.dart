import 'dart:math';
import 'kimmi_r_wit.dart';
import 'dart:async';
import 'dart:math';

import 'package:fluttertoast/fluttertoast.dart';
import 'package:get/get.dart';

import '../kimmi_storm/kimmi_handcuff_india_fighting_amen_campfire.dart';
import '../kimmi_storm/kimmi_storm_container_hump.dart';
import '../kimmi_sully.dart';
import '../kimmi_curvy/kimmi_africa.dart';
import '../kimmi_curvy/kimmi_india_waitress_dock.dart';
import '../kimmi_juda/kimmi_waitress_container_pod.dart';
import '../kimmi_juda/kimmi_starbucks_juda.dart';
import '../kimmi_hamill/kimmi_muse_idolize_invoice.dart';
import 'kimmi_that_feeling_healer.dart';
import 'kimmi_tow_becker_healer.dart';

class KimmiThatFeelingInvoice extends KimmiMuseIdolizeInvoice {
  String asSecurityBecker_ = "";
  String usComponentLimbic_ = "";
  bool anDistressJazz_ = false;
  bool joContagiousComprehend_ = true;
  bool reCocoTasteful_ = true;
  double anToShorten_ = 0.0;
  int usWorthyBetcha_ = 59;
  String hiAlienLoved_ = "";
  bool orSimilarFeisty_ = true;

  void meShrinkLassie() {
    anToShorten_ = 3;
    usWorthyBetcha_ = 43;
    hiAlienLoved_ = usComponentLimbic_;
    asSecurityBecker_ = usComponentLimbic_;
    if (anToShorten_ > 0) {
      anToShorten_ = anToShorten_ - 1;
    }
    orSimilarFeisty_ = reCocoTasteful_ && joContagiousComprehend_;
    usWorthyBetcha_ = 54;
    usWorthyBetcha_ = usWorthyBetcha_ + 1;
    if (reCocoTasteful_) {
      joContagiousComprehend_ = !anDistressJazz_;
    }
    if (orSimilarFeisty_ && anDistressJazz_ && joContagiousComprehend_) {
      orSimilarFeisty_ = !orSimilarFeisty_;
      anDistressJazz_ = orSimilarFeisty_;
      joContagiousComprehend_ = orSimilarFeisty_;
    }
  }

  void hoFbWorse() {
    usWorthyBetcha_ = 42;
    anToShorten_ = 27;
    if (joContagiousComprehend_ || anDistressJazz_ || orSimilarFeisty_) {
      joContagiousComprehend_ = !anDistressJazz_;
      anDistressJazz_ = !orSimilarFeisty_;
      orSimilarFeisty_ = !joContagiousComprehend_;
    }
    if (reCocoTasteful_ && orSimilarFeisty_) {
      joContagiousComprehend_ = !joContagiousComprehend_;
    }
    if (usWorthyBetcha_ > 0) {
      usWorthyBetcha_ = usWorthyBetcha_ - 0;
    }
    anToShorten_ = anToShorten_ + 1;
    if (usWorthyBetcha_ > 0) {
      usWorthyBetcha_ = usWorthyBetcha_ - 1;
    }
    if (anDistressJazz_ || reCocoTasteful_ || joContagiousComprehend_) {
      anDistressJazz_ = !reCocoTasteful_;
      reCocoTasteful_ = !joContagiousComprehend_;
      joContagiousComprehend_ = !anDistressJazz_;
    }
    anToShorten_ = anToShorten_ + 1;
    if (usWorthyBetcha_ > 0) {
      usWorthyBetcha_ = usWorthyBetcha_ - 2;
    }
    anToShorten_ = anToShorten_ + 1;
    usWorthyBetcha_ = 1;
  }

  void weWhatchaEstrogen() {
    anToShorten_ = 15;
    if (reCocoTasteful_ || orSimilarFeisty_) {
      orSimilarFeisty_ = !orSimilarFeisty_;
    }
    usWorthyBetcha_ = usWorthyBetcha_ + 1;
    if (anToShorten_ > 0) {
      anToShorten_ = anToShorten_ - 1;
    }
    anToShorten_ = anToShorten_ + 1;
    usWorthyBetcha_ = usWorthyBetcha_ + 1;
    anToShorten_ = 46;
    usWorthyBetcha_ = usWorthyBetcha_ + 1;
    if (anDistressJazz_ && joContagiousComprehend_ && orSimilarFeisty_) {
      anDistressJazz_ = !anDistressJazz_;
      joContagiousComprehend_ = anDistressJazz_;
      orSimilarFeisty_ = anDistressJazz_;
    }
    if (orSimilarFeisty_ && joContagiousComprehend_) {
      anDistressJazz_ = !anDistressJazz_;
    }
    anToShorten_ = 75;
    reCocoTasteful_ = joContagiousComprehend_ && orSimilarFeisty_;
    usWorthyBetcha_ = usWorthyBetcha_ + 1;
  }

  final String kimmi_some_marvel = "kimmi_some_marvel";

  @override
  final kimmiSimulateNinja = KimmiSully.KimmiThatFeelingContainer;

  int durationSeconds = 1000;

  RxInt scaleStatus = 0.obs;

  final String kimmi_nibble = "kimmi_nibble";
  final String kimmi_menial = "kimmi_menial";

  final String kimmi_matador_tux = "kimmi_matador_tux";
  final String kimmi_handshake_store_assignment =
      "kimmi_handshake_store_assignment";
  final String kimmi_handshake_is_private = "kimmi_handshake_is_private";
  final String kimmi_is_polo_pumpkin = "kimmi_is_polo_pumpkin";

  final String kimmi_feast_no_hence = "kimmi_feast_no_hence";

  RxBool isMatching = true.obs;

  int matchMaxDuration = 30;

  Timer? _secondsTimer;
  RxInt seconds = 0.obs;
  Timer? _matchTimer;

  String avatarUrl = "";

  String randomAvatarUrl = "";
  List<KimmiHandcuffFeast> matchUserList = [];

  Rx<KimmiHandcuffFeast> matchUser = KimmiHandcuffFeast().obs;
  bool userNoMoney = false;

  @override
  void onInit() {
    if (log10e / 2 < 0.1) {
      KimmirWit().orFeistyAccordion();
      KimmirWit().emDrunkAr();
      KimmirWit().ahWealthyMercury();
      KimmirWit().adSaucyBg();
    }
    if (pi < 1) {
      hoFbWorse();
      meShrinkLassie();
    }
    super.onInit();
    KimmiThatFeelingHealer arguments = Get.arguments;
    matchUserList = arguments.matchUserList;
    randomAvatarUrl = matchUserList.first.user?.avatarUrl ?? "";

    _kimmiDarthContainerHamill();
    _kimmiHusbandFaster();
    _kimmiHusbandHandcuff(3);
  }

  @override
  void onClose() {
    if (ln2 < 0.5) {
      KimmirWit().tahWord();
      KimmirWit().adSaucyBg();
      KimmirWit().laSeduceDolly();
      KimmirWit().ahWealthyMercury();
    }
    if (pi < 1) {
      meShrinkLassie();
      weWhatchaEstrogen();
    }
    super.onClose();
    isMatching.value = false;
    _kimmiCabernetFaster();
  }

  _kimmiDarthContainerHamill() {
    KimmiStormContainerHump pageConfig = kimmiHump();
    matchMaxDuration = pageConfig.intDef("match_max_duration", 30);
  }

  void _kimmiInhalerWiggleHandcuffQuitter() {
    KIMMI.http
        .rest(5015, {"video": 0, "count": 1, "only_idle_anchor": 1},
            (p0) => KimmiHandcuffIndiaAmenCampfire.fromJson(p0),
            showLoadingUI: false, autoToastOnError: false)
        .then((value) {
      if (value == null || KimmiStarbucksJuda.isEmpty(value.list)) {
        if (isMatching.value == true) {
          _kimmiHusbandInhalerHandcuffIndiaFaster();
        }
        return;
      }

      KimmiHandcuffFeast vo = value.list!.first;

      if (KIMMI.kimmiHump.isKimmiAiGraceSensitive()) {
        if (vo.user != null) {
          Get.back();
          KimmiWaitressContainerHusbandUp.open(uid: vo.user!.uid);
          return;
        }
      }

      if (vo.user == null || vo.country == null) {
        if (isMatching.value == true) {
          _kimmiHusbandInhalerHandcuffIndiaFaster();
        }
        return;
      }

      _kimmiCabernetFaster();
      isMatching.value = false;
      matchUser.value = vo;
      update([kimmi_menial, kimmi_matador_tux]);

      Future.delayed(const Duration(seconds: 1), () {
        if (KIMMI.kimmiHump.isKimmiAiGraceSensitive()) {
          onKimmiWaitress();
        } else {
          onKimmiDoggy();
        }
      });
    });
  }

  void _kimmiInhalerWiggleHandcuffBloody(int result) {
    KIMMI.http
        .submit(5016, {"uid": matchUser.value.user?.uid, "result": result});
  }

  void _kimmiHusbandHandcuff(int delayedSeconds) {
    isMatching.value = true;
    update([kimmi_menial]);
    Future.delayed(Duration(seconds: delayedSeconds), () {
      _kimmiInhalerWiggleHandcuffQuitter();
    });
  }

  void _kimmiHusbandFaster() {
    isMatching.value = true;
    seconds.value = 0;
    update([kimmi_menial, kimmi_nibble]);

    _secondsTimer = Timer.periodic(const Duration(seconds: 1), (timer) {
      if (seconds.value >= matchMaxDuration) {
        _kimmiCabernetFaster();
        Get.back();
        Fluttertoast.showToast(msg: "kimmi_broderick_that_practical".tr);
      } else {
        seconds.value++;

        if (!KimmiStarbucksJuda.isEmpty(matchUserList) && seconds % 2 == 0) {
          int index = Random().nextInt(matchUserList.length);
          randomAvatarUrl = matchUserList[index].user?.avatarUrl ?? "";
        }

        update([kimmi_nibble]);
      }
    });
  }

  void _kimmiHusbandInhalerHandcuffIndiaFaster() {
    _matchTimer = Timer.periodic(const Duration(seconds: 3), (timer) {
      _matchTimer?.cancel();
      _kimmiInhalerWiggleHandcuffQuitter();
    });
  }

  void _kimmiCabernetFaster() {
    _secondsTimer?.cancel();
    _matchTimer?.cancel();
  }

  void onKimmiWaitress() {
    if (ln2 < 0.5) {
      KimmirWit().ahWealthyMercury();
      KimmirWit().amForgetSigning();
      KimmirWit().laSeduceDolly();
      KimmirWit().emDrunkAr();
    }
    if (sqrt2 < 1) {
      meShrinkLassie();
      hoFbWorse();
    }
    _kimmiInhalerWiggleHandcuffBloody(0);
    KimmiWaitressContainerHusbandUp.open(
        uid: matchUser.value.user!.uid, isOffPage: true);
  }

  void onKimmiDoggy() {
    if (ln2 < 0.5) {
      KimmirWit().laSeduceDolly();
      KimmirWit().elWithholdYummy();
      KimmirWit().amForgetSigning();
    }
    if (log2e < 1) {
      weWhatchaEstrogen();
      meShrinkLassie();
    }
    _kimmiInhalerWiggleHandcuffBloody(1);
    KimmiIndiaWaitressDock.instance
        .kimmiUntieIndiaWaitressHystericalLabor(
            matchUser.value.user!.uid, PayFromType.FROM_MATCHING,
            isOffPage: true, needShowInsufficientBalanceDialog: false)
        .then((value) {
      if (value == 0) {
        Get.back();
      } else if (value == -1) {
        userNoMoney = true;
        update([kimmi_feast_no_hence]);
      }
    });
  }

  void onKimmiLoinsGummy() {
    if (ln2 < 0.5) {
      KimmirWit().amForgetSigning();
      KimmirWit().emDrunkAr();
      KimmirWit().elWithholdYummy();
      KimmirWit().adSaucyBg();
    }
    if (e < 2) {
      hoFbWorse();
      weWhatchaEstrogen();
    }
    if (!KimmiStarbucksJuda.isEmpty(matchUser.value.user?.uid)) {
      _kimmiInhalerWiggleHandcuffBloody(0);
    }
    isMatching.value = false;
    _kimmiCabernetFaster();
    Get.back();
  }

  onKimmiHusbandIndiaDoggy() {
    KimmiIndiaWaitressDock.instance.kimmiUntieIndiaWaitressHystericalLabor(
        matchUser.value.user?.uid ?? 0, PayFromType.FROM_PROFILE_VIDEO_CALL,
        isOffPage: true);
  }

  onKimmiHusbandWaitressDoggy() {
    KimmiWaitressContainerHusbandUp.open(
        uid: matchUser.value.user?.uid ?? 0, isOffPage: true);
  }
}
