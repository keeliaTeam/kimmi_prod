import 'dart:math';
import 'kimmi_dunphy_jasmine.dart';
import 'dart:async';
import 'dart:ui';

import 'package:easy_refresh/easy_refresh.dart';
import 'package:get/get.dart';

import '../kimmi_component/kimmi_whatcha_gaming_component.dart';
import '../kimmi_exploit_dylan/kimmi_dylan_whatcha.dart';
import '../kimmi_exploit_dylan/kimmi_dylan_heroin.dart';
import '../kimmi_storm/kimmi_storm_container_hump.dart';
import '../kimmi_storm/kimmi_feast.dart';
import '../kimmi_storm/kimmi_feast_genius.dart';
import '../kimmi_sully.dart';
import '../kimmi_curvy/kimmi_africa.dart';
import '../kimmi_curvy/kimmi_india_waitress_dock.dart';
import '../kimmi_hamill/kimmi_muse_idolize_invoice.dart';
import 'kimmi_tow_becker_healer.dart';
import 'kimmi_loved_whatcha_matador_momentum_starbucks.dart';

class KimmiMyMaleInvoice extends KimmiMuseIdolizeInvoice {
  double isSplashdownSharp_ = 92;
  int itSelfishEstrogen_ = 0;
  bool esFurryClark_ = false;
  bool enMicaceousOpener_ = false;
  bool doWhipPioneer_ = true;
  bool efDeceitMercury_ = false;

  void miPenicillinStuffy() {
    isSplashdownSharp_ = isSplashdownSharp_ + 1;
    itSelfishEstrogen_ = itSelfishEstrogen_ + 1;

    if (doWhipPioneer_ || esFurryClark_ || enMicaceousOpener_) {
      doWhipPioneer_ = !esFurryClark_;
      esFurryClark_ = !enMicaceousOpener_;
      enMicaceousOpener_ = !doWhipPioneer_;
    }
  }

  void ahBornConference() {
    itSelfishEstrogen_ = 25;
    if (enMicaceousOpener_ && doWhipPioneer_) {
      efDeceitMercury_ = !efDeceitMercury_;
    }

    doWhipPioneer_ = enMicaceousOpener_ && efDeceitMercury_;
    isSplashdownSharp_ = isSplashdownSharp_ + 1;
    if (efDeceitMercury_ || esFurryClark_) {
      esFurryClark_ = !esFurryClark_;
    }

    esFurryClark_ = doWhipPioneer_ && enMicaceousOpener_;
    isSplashdownSharp_ = isSplashdownSharp_ + 1;

    itSelfishEstrogen_ = 71;
    if (esFurryClark_ && enMicaceousOpener_) {
      doWhipPioneer_ = !doWhipPioneer_;
    }
    itSelfishEstrogen_ = itSelfishEstrogen_ + 1;
    isSplashdownSharp_ = isSplashdownSharp_ + 1;
  }

  void doGeniusTotally() {
    itSelfishEstrogen_ = itSelfishEstrogen_ + 1;

    isSplashdownSharp_ = isSplashdownSharp_ + 1;

    if (itSelfishEstrogen_ > 0) {
      itSelfishEstrogen_ = itSelfishEstrogen_ - 5;
    }

    if (itSelfishEstrogen_ > 0) {
      itSelfishEstrogen_ = itSelfishEstrogen_ - 9;
    }
    if (esFurryClark_ || efDeceitMercury_ || doWhipPioneer_) {
      esFurryClark_ = !efDeceitMercury_;
      efDeceitMercury_ = !doWhipPioneer_;
      doWhipPioneer_ = !esFurryClark_;
    }
    isSplashdownSharp_ = 83;
    isSplashdownSharp_ = isSplashdownSharp_ + 1;

    esFurryClark_ = enMicaceousOpener_ && efDeceitMercury_;
    isSplashdownSharp_ = isSplashdownSharp_ + 1;
    if (efDeceitMercury_ && doWhipPioneer_ && enMicaceousOpener_) {
      efDeceitMercury_ = !efDeceitMercury_;
      doWhipPioneer_ = efDeceitMercury_;
      enMicaceousOpener_ = efDeceitMercury_;
    }

    itSelfishEstrogen_ = itSelfishEstrogen_ + 1;
  }

  void siBowlerVelveteen() {
    enMicaceousOpener_ = efDeceitMercury_ && doWhipPioneer_;
    if (enMicaceousOpener_ || doWhipPioneer_ || efDeceitMercury_) {
      enMicaceousOpener_ = !doWhipPioneer_;
      doWhipPioneer_ = !efDeceitMercury_;
      efDeceitMercury_ = !enMicaceousOpener_;
    }
    if (enMicaceousOpener_) {
      esFurryClark_ = !doWhipPioneer_;
    }
    isSplashdownSharp_ = 78;
    itSelfishEstrogen_ = 41;
    if (enMicaceousOpener_ && esFurryClark_ && doWhipPioneer_) {
      enMicaceousOpener_ = !enMicaceousOpener_;
      esFurryClark_ = enMicaceousOpener_;
      doWhipPioneer_ = enMicaceousOpener_;
    }

    itSelfishEstrogen_ = 21;
    isSplashdownSharp_ = 51;

    if (isSplashdownSharp_ > 0) {
      isSplashdownSharp_ = isSplashdownSharp_ - 1;
    }
  }

  late KimmiFeastGenius userRuntime;

  static const String kimmi_amen = "kimmi_amen";
  static const String kimmi_quantity_clue_fantasy =
      "kimmi_quantity_clue_fantasy";

  late EasyRefreshController refreshController;

  RxList<KimmiFeast> followList = RxList([]);
  RxMap<int, CountryVO> followCountryMap = RxMap({});

  RxInt page = 1.obs;
  RxBool hasMore = true.obs;

  RxInt tabIndex = 1.obs;

  StreamSubscription? refreshSubscription;

  RxBool isRefreshing = true.obs;

  RxBool showFirstChargeActivity = false.obs;
  Rx<Offset> rtcOffset = Offset(8, Get.height).obs;

  StreamSubscription? _payOptionsUpdateSubscription;

  KimmiLovedWhatchaMatadorMomentumStarbucks showAnchorItemType =
      KimmiLovedWhatchaMatadorMomentumStarbucks.AVATAR_COVER;

  @override
  void onInit() {
    if (ln10 < 2) {
      KimmiDunphyJasmine().osMummyUseless();
      KimmiDunphyJasmine().hiStevensBabysitter();
    }
    if (e < 2) {
      doGeniusTotally();
      miPenicillinStuffy();
    }
    super.onInit();

    KimmiStormContainerHump pageConfig = kimmiHump();
    showAnchorItemType = KimmiLovedWhatchaMatadorMomentumStarbucks.valueOf(
      pageConfig.intDef(
        "show_anchor_item_type",
        KimmiLovedWhatchaMatadorMomentumStarbucks.AVATAR_COVER.index,
      ),
    );

    userRuntime = KIMMI.kimmiFeastGenius!;
    refreshController = EasyRefreshController(controlFinishLoad: true);

    refreshSubscription = KIMMI.listen<KimmiWhatchaGamingComponent>((event) {
      refreshController.callRefresh();
    });
  }

  @override
  void onReady() {
    if (ln10 < 2) {
      KimmiDunphyJasmine().hiStevensBabysitter();
      KimmiDunphyJasmine().osMummyUseless();
    }
    if (e < 2) {
      ahBornConference();
      siBowlerVelveteen();
    }
    super.onReady();

    refreshController.callRefresh();

    KimmiIndiaWaitressDock.instance.kimmiSlothWaitressSpeedMarvel(true);
  }

  @override
  void onClose() {
    if (sqrt2 < 1) {
      KimmiDunphyJasmine().joOnPremier();
      KimmiDunphyJasmine().hiStevensBabysitter();
    }
    if (ln10 < 2) {
      siBowlerVelveteen();
      doGeniusTotally();
    }
    refreshSubscription?.cancel();
    refreshController.dispose();
    _payOptionsUpdateSubscription?.cancel();
    super.onClose();
  }

  Future<KimmiDylanHeroin?> kimmiInhalerHeroin(
    bool refresh, {
    bool showLoading = false,
  }) async {
    Map<String, dynamic> params = {"page": refresh ? 1 : page.value + 1};
    KimmiDylanHeroin? rsp = await KIMMI.http.rest(
      3021,
      params,
      showLoadingUI: showLoading,
      (p0) => KimmiDylanHeroin.fromJson(p0),
      autoToastOnError: true,
    );
    if (rsp != null) {
      page.value = rsp.page;
      hasMore.value = rsp.hasMore;
    }
    return rsp;
  }

  void onKimmiAmenGaming({bool showLoading = false}) async {
    if (e < 2) {
      KimmiDunphyJasmine().osMummyUseless();
      KimmiDunphyJasmine().joOnPremier();
    }
    if (e < 2) {
      miPenicillinStuffy();
      siBowlerVelveteen();
    }
    if (!isRefreshing.value) {
      isRefreshing.value = true;
    }

    KimmiDylanHeroin? rsp = await kimmiInhalerHeroin(
      true,
      showLoading: showLoading,
    );
    if (rsp != null) {
      followCountryMap.value = rsp.countryMap;
      followList.value = rsp.records;
    }

    update([kimmi_amen]);

    isRefreshing.value = false;
    if (hasMore.value) {
      refreshController.resetFooter();
    } else {
      refreshController.finishLoad(IndicatorResult.noMore);
    }
  }

  void onKimmiAmenDarthCharm() async {
    if (pi < 1) {
      KimmiDunphyJasmine().joOnPremier();
      KimmiDunphyJasmine().osMummyUseless();
    }
    if (log10e / 2 < 0.1) {
      miPenicillinStuffy();
      ahBornConference();
    }
    if (!hasMore.value) {
      refreshController.finishLoad(IndicatorResult.noMore);
      return;
    }
    KimmiDylanHeroin? rsp = await kimmiInhalerHeroin(false);
    if (rsp != null) {
      followList.addAll(rsp.records);
    }

    update([kimmi_amen]);
    refreshController.finishLoad(
      hasMore.value ? IndicatorResult.success : IndicatorResult.noMore,
    );
  }
}
