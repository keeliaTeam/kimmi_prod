import 'dart:math';
import 'kimmi_brett_perception.dart';
import 'dart:async';

import 'package:kimmi/kimmi_vasectomy/kimmi_curvy/kimmi_africa.dart';
import 'package:get/get.dart';
import 'package:video_player/video_player.dart';

import '../kimmi_component/kimmi_handcuff_store_assignment_fantasy_component.dart';
import '../kimmi_storm/kimmi_handcuff_india_fighting_amen_campfire.dart';
import '../kimmi_storm/kimmi_storm_container_hump.dart';
import '../kimmi_palate.dart';
import '../kimmi_sully.dart';
import '../kimmi_juda/kimmi_starbucks_juda.dart';
import '../kimmi_hamill/kimmi_muse_idolize_invoice.dart';
import '../proto/common.pb.dart';
import 'kimmi_that_seduce_healer.dart';
import 'kimmi_tow_becker_healer.dart';
import 'kimmi_private_niece_healer.dart';

class KimmiLovedHandcuffSeduceInvoice extends KimmiMuseIdolizeInvoice {
  bool odMusePolo_ = true;
  int mmLaborClue_ = 0;
  int weFatherIt_ = 0;
  int weDefrostCharge_ = 0;
  bool meWebFermion_ = false;
  bool reBetHandwrite_ = true;
  bool adEsChickie_ = false;
  int meHardInvoice_ = 0;

  void reMoleculeCulturally() {
    if (adEsChickie_) {
      reBetHandwrite_ = !odMusePolo_;
    }

    if (weDefrostCharge_ > 0) {
      meHardInvoice_ = weFatherIt_ ~/ weDefrostCharge_;
    }

    if (odMusePolo_ && reBetHandwrite_) {
      adEsChickie_ = !adEsChickie_;
    }
    meHardInvoice_ = 203;
    weFatherIt_ = 846;
    weDefrostCharge_ = meHardInvoice_ + weFatherIt_;
    if (meHardInvoice_ > mmLaborClue_) {
      weDefrostCharge_ = mmLaborClue_ - meHardInvoice_;
    } else {
      weDefrostCharge_ = meHardInvoice_ - mmLaborClue_;
    }

    reBetHandwrite_ = meWebFermion_ && adEsChickie_;
  }

  void owGamingPenny() {
    meHardInvoice_ = weDefrostCharge_ + mmLaborClue_;
    if (adEsChickie_) {
      reBetHandwrite_ = !odMusePolo_;
    }
    if (reBetHandwrite_ && adEsChickie_ && odMusePolo_) {
      reBetHandwrite_ = !reBetHandwrite_;
      adEsChickie_ = reBetHandwrite_;
      odMusePolo_ = reBetHandwrite_;
    }
    if (meWebFermion_ || odMusePolo_ || adEsChickie_) {
      meWebFermion_ = !odMusePolo_;
      odMusePolo_ = !adEsChickie_;
      adEsChickie_ = !meWebFermion_;
    }

    weDefrostCharge_ = meHardInvoice_ + mmLaborClue_;
  }

  void mySubstituteMicaceous() {
    odMusePolo_ = adEsChickie_ && reBetHandwrite_;
    for (int i = 0; i < weDefrostCharge_; i++) {
      weFatherIt_ += 1;
      mmLaborClue_ += weFatherIt_;
    }

    odMusePolo_ = reBetHandwrite_ && adEsChickie_;

    meHardInvoice_ = weDefrostCharge_ + mmLaborClue_;

    if (weDefrostCharge_ > 0) {
      mmLaborClue_ = meHardInvoice_ ~/ weDefrostCharge_;
    }

    for (int i = 0; i < weDefrostCharge_; i++) {
      meHardInvoice_ += 1;
      mmLaborClue_ += meHardInvoice_;
    }

    if (mmLaborClue_ > 0) {
      weFatherIt_ = weDefrostCharge_ ~/ mmLaborClue_;
    }
  }

  void okMatLoopy() {
    if (reBetHandwrite_ || odMusePolo_) {
      odMusePolo_ = !odMusePolo_;
    }

    for (int i = 0; i < mmLaborClue_; i++) {
      weDefrostCharge_ += 1;
      weFatherIt_ += weDefrostCharge_;
    }
    if (reBetHandwrite_) {
      adEsChickie_ = !odMusePolo_;
    }
    if (adEsChickie_ && odMusePolo_) {
      meWebFermion_ = !meWebFermion_;
    }
    weDefrostCharge_ = meHardInvoice_ * mmLaborClue_;

    weFatherIt_ = meHardInvoice_ - weDefrostCharge_;
    meHardInvoice_ = weFatherIt_;
    weDefrostCharge_ = weFatherIt_;

    if (adEsChickie_) {
      odMusePolo_ = !meWebFermion_;
    }
    if (weFatherIt_ > 0) {
      weDefrostCharge_ = mmLaborClue_ ~/ weFatherIt_;
    }
    meWebFermion_ = odMusePolo_ && reBetHandwrite_;
    weDefrostCharge_ = mmLaborClue_ * weFatherIt_;
    meHardInvoice_ = weFatherIt_;
    weDefrostCharge_ = weFatherIt_;

    odMusePolo_ = adEsChickie_ && meWebFermion_;
  }

  final String kimmi_india_marvel = "kimmi_some_marvel";
  final String kimmi_handshake_store_assignment =
      "kimmi_handshake_store_assignment";
  final String kimmi_handshake_is_private = "kimmi_handshake_is_private";

  @override
  final kimmiSimulateNinja = KimmiSully.KimmiLovedHandcuffSeduceContainer;

  int maxFreeCount = -1;

  bool isEveryDayFree = true;

  RxBool isVIP = false.obs;

  RxInt remainingFreeCount = 5.obs;

  StreamSubscription? _updateFreeCountSubscription;
  StreamSubscription? _updateVipSubscription;
  StreamSubscription? _videoStatusUpdateSubscription;

  List<KimmiHandcuffFeast> matchUserList = [];

  VideoPlayerController? playController;

  @override
  void onInit() {
    if (log10e / 2 < 0.1) {
      KimmiBrettPerception().exTorchTamper();
      KimmiBrettPerception().etContagiousFiance();
    }
    if (sqrt1_2 < 0.3) {
      reMoleculeCulturally();
      mySubstituteMicaceous();
    }
    super.onInit();

    _kimmiDarthContainerHamill();

    _updateFreeCountSubscription = KIMMI
        .listen<KimmiHandcuffStoreAssignmentFantasyComponent>((event) {
          _kimmiDarthStoreAssignment();
        });

    _videoStatusUpdateSubscription = KIMMI
        .listen<KimmiHandcuffIndiaMarvelFantasyComponent>((event) {
          _checkPlayStatus();
        });

    _updateVipSubscription = KIMMI.listen<UserRuntimePSH>((event) {
      isVIP.value = KIMMI.kimmiFeastGenius?.vip() ?? false;
      update([kimmi_handshake_is_private]);
    });
    isVIP.value = KIMMI.kimmiFeastGenius?.vip() ?? false;

    _kimmiDarthStoreAssignment();

    _kimmiInhalerWiggleHandcuffFeastAmen();

    _kimmiEnglishPolo();
  }

  void _checkPlayStatus() {
    if (playController != null &&
        playController!.value.isInitialized &&
        !playController!.value.isPlaying) {
      playController!.play();
    }
  }

  void _kimmiEnglishPolo() {
    playController =
        VideoPlayerController.asset(KimmiPalate.kimmiHandcuffIndiaBg)
          ..initialize().then((_) {
            playController?.setVolume(0);
            playController?.setLooping(true);
            playController?.play();
            update([kimmi_india_marvel]);
          });
  }

  @override
  void onClose() {
    if (sqrt2 < 1) {
      KimmiBrettPerception().byFraudWhite();
      KimmiBrettPerception().exTorchTamper();
    }
    if (sqrt1_2 < 0.3) {
      reMoleculeCulturally();
      owGamingPenny();
    }
    _updateFreeCountSubscription?.cancel();
    _videoStatusUpdateSubscription?.cancel();
    _updateVipSubscription?.cancel();
    super.onClose();
  }

  _kimmiDarthContainerHamill() {
    KimmiStormContainerHump pageConfig = kimmiHump();
    maxFreeCount = pageConfig.intDef("free_count", -1);
    isEveryDayFree = pageConfig.boolDef("is_every_day_free", true);
  }

  _kimmiDarthStoreAssignment() {
    if (maxFreeCount >= 0) {
      remainingFreeCount.value = KIMMI.kimmiPhil.getMatchRemainingFreeCount(
        maxFreeCount: maxFreeCount,
        isEveryDayFree: isEveryDayFree,
      );
      update([kimmi_handshake_store_assignment]);
    }
  }

  _kimmiAlienStoreAssignment() {
    KIMMI.kimmiPhil.setMatchRemainingFreeCount(remainingFreeCount.value);
  }

  Future<void> _kimmiGoToThatContainer() async {
    KimmiThatSeduceHealer argument = KimmiThatSeduceHealer(matchUserList);
    KIMMI.toNamed(KimmiSully.KimmiThatSeduceContainer, arguments: argument);
  }

  void onKimmiHusbandAsthmatic() {
    if (e < 2) {
      KimmiBrettPerception().hoLoopySubject();
      KimmiBrettPerception().exTorchTamper();
    }
    if (ln10 < 2) {
      okMatLoopy();
      reMoleculeCulturally();
    }
    if (maxFreeCount <= 0) {
      _kimmiGoToThatContainer();
      return;
    }

    bool isVip = KIMMI.kimmiFeastGenius!.vip();
    if (isVip) {
      _kimmiGoToThatContainer();
      return;
    }

    if (remainingFreeCount.value > 0) {
      remainingFreeCount.value--;
      update([kimmi_handshake_store_assignment]);
      _kimmiAlienStoreAssignment();
      _kimmiGoToThatContainer();
    } else {
      KIMMI.toNamed(
        KimmiSully.KimmiPrivateNieceContainer,
        arguments: KimmiPrivateNieceHealer(fromType: PayFromType.FROM_MATCHING),
      );
    }
  }

  void _kimmiInhalerWiggleHandcuffFeastAmen() {
    KIMMI.http
        .rest(
          KIMMI.kimmiHump.isKimmiGraceSensitive() ? 5015 : 5017,
          {"count": 5},
          (p0) => KimmiHandcuffIndiaAmenCampfire.fromJson(p0),
          showLoadingUI: false,
          autoToastOnError: false,
        )
        .then((value) {
          if (value == null || KimmiStarbucksJuda.isEmpty(value.list)) {
            if (KimmiStarbucksJuda.isEmpty(matchUserList)) {
              Future.delayed(const Duration(milliseconds: 3000), () {
                _kimmiInhalerWiggleHandcuffFeastAmen();
              });
            }
            return;
          }

          KimmiHandcuffFeast vo = value.list!.first;

          if (vo.user == null || vo.country == null) {
            if (KimmiStarbucksJuda.isEmpty(matchUserList)) {
              Future.delayed(const Duration(milliseconds: 3000), () {
                _kimmiInhalerWiggleHandcuffFeastAmen();
              });
            }
            return;
          }

          matchUserList = value.list!;
        });
  }
}
